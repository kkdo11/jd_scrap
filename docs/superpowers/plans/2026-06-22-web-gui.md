# 웹 GUI (조각 A) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기존 CLI 채용 매칭 도구를 브라우저에서 이력서 붙여넣기→실시간 진행률→결과 카드로 쓸 수 있는 웹 앱으로 확장한다.

**Architecture:** 수집→채점 오케스트레이션을 `pipeline.ts`로 분리해 CLI와 웹이 공유한다. 진행 상황은 `onProgress` 콜백으로 emit하고, CLI는 콜백→console.log, 웹은 콜백→SSE(`fetch` POST 응답 스트림)로 연결한다. Express 서버가 화면을 서빙하고 단일 실행 잠금으로 GPU 1개 현실을 반영한다. 외부 I/O(원티드 API·Ollama)는 의존성 주입으로 갈아끼워 순수 단위 테스트가 가능하게 한다.

**Tech Stack:** Node + TypeScript(tsx), Express 4, 바닐라 프론트엔드(fetch 스트리밍 SSE), Node 내장 `node:test`. 신규 런타임 의존성은 `express` 1개.

## Global Constraints

- **기존 동작 회귀 금지:** `wanted.ts`·`reporter.ts`는 수정하지 않는다. `scorer.ts`는 진행 출력 부분만 콜백화하되, 콜백 미전달 시 기존 동작과 동일해야 한다.
- **신규 런타임 의존성은 `express` 단 1개.** 그 외는 Node 내장 모듈만 사용(테스트 러너 `node:test`, `http`, `fs`, `path`).
- **테스트는 외부 I/O를 호출하지 않는다.** 원티드 API·Ollama LLM은 의존성 주입으로 가짜 함수를 넘겨 검증한다.
- **포트:** `process.env.PORT ?? 3000`.
- **limit 범위:** 1~100, 잘못된 값(0·음수·비숫자)은 50으로.
- **CSS:** 원티드(wanted.co.kr)풍 라이트 테마 — 흰 배경, 둥근 카드, 원티드 블루 포인트색, Pretendard 폰트.
- **언어:** 모든 UI 문구·커밋 메시지 본문은 한국어, 커밋 prefix는 영어 conventional commits.

---

### Task 1: ProgressEvent 타입 + SSE 포맷터 + 테스트 인프라

**Files:**
- Modify: `src/types.ts` (ProgressEvent 추가)
- Create: `src/sse.ts`
- Test: `src/sse.test.ts`
- Modify: `package.json` (test 스크립트 추가)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `ProgressEvent` (types.ts) — 아래 union 타입
  - `formatSSE(event: ProgressEvent): string` (sse.ts)

- [ ] **Step 1: 실패하는 테스트 작성**

`src/sse.test.ts`:
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { formatSSE } from './sse';

test('formatSSE: event 이름과 data JSON을 SSE 규격으로 직렬화', () => {
  const out = formatSSE({ type: 'status', message: '수집 중' });
  assert.equal(out, 'event: status\ndata: {"type":"status","message":"수집 중"}\n\n');
});

test('formatSSE: scored 이벤트도 전체 객체를 data로 직렬화', () => {
  const job = {
    id: 1, position: 'BE', companyName: 'A', location: '서울',
    mainTasks: '', requirements: '', preferredPoints: '',
    score: 80, matchPoints: [], gaps: [], summary: 's',
  };
  const out = formatSSE({ type: 'scored', index: 1, total: 3, job });
  assert.ok(out.startsWith('event: scored\ndata: {'));
  assert.ok(out.endsWith('}\n\n'));
});
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

Run: `npx tsx --test src/sse.test.ts`
Expected: FAIL — `Cannot find module './sse'`

- [ ] **Step 3: 타입 추가**

`src/types.ts` 끝에 추가:
```ts
export type ProgressEvent =
  | { type: 'status'; message: string }
  | { type: 'scored'; index: number; total: number; job: ScoredJob }
  | { type: 'done'; count: number }
  | { type: 'error'; message: string };
```

- [ ] **Step 4: 포맷터 구현**

`src/sse.ts`:
```ts
import { ProgressEvent } from './types';

// ProgressEvent를 SSE 텍스트 프레임으로 직렬화한다.
// event 이름 = type, data = 이벤트 객체 전체(JSON).
export function formatSSE(event: ProgressEvent): string {
  return `event: ${event.type}\ndata: ${JSON.stringify(event)}\n\n`;
}
```

- [ ] **Step 5: package.json에 test 스크립트 추가**

`package.json`의 `scripts`에 추가:
```json
    "test": "tsx --test src/**/*.test.ts"
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `npx tsx --test src/sse.test.ts`
Expected: PASS (2 tests)

- [ ] **Step 7: 커밋**

```bash
git add src/types.ts src/sse.ts src/sse.test.ts package.json
git commit -m "feat: ProgressEvent 타입과 SSE 포맷터 추가"
```

---

### Task 2: 입력 검증 헬퍼 (clampLimit / isValidResume)

**Files:**
- Create: `src/validation.ts`
- Test: `src/validation.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `clampLimit(raw: unknown): number`
  - `isValidResume(resume: unknown): resume is string`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/validation.test.ts`:
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { clampLimit, isValidResume } from './validation';

test('clampLimit: 정상 범위는 그대로', () => {
  assert.equal(clampLimit(30), 30);
  assert.equal(clampLimit(1), 1);
  assert.equal(clampLimit(100), 100);
});

test('clampLimit: 잘못된 값은 50', () => {
  assert.equal(clampLimit(0), 50);
  assert.equal(clampLimit(-5), 50);
  assert.equal(clampLimit('abc'), 50);
  assert.equal(clampLimit(undefined), 50);
  assert.equal(clampLimit(null), 50);
});

test('clampLimit: 100 초과는 100으로, 소수는 내림', () => {
  assert.equal(clampLimit(101), 100);
  assert.equal(clampLimit(999), 100);
  assert.equal(clampLimit(50.7), 50);
});

test('clampLimit: 숫자 문자열도 허용', () => {
  assert.equal(clampLimit('30'), 30);
});

test('isValidResume: 비어있지 않은 문자열만 true', () => {
  assert.equal(isValidResume('내 이력서'), true);
  assert.equal(isValidResume(''), false);
  assert.equal(isValidResume('   '), false);
  assert.equal(isValidResume(undefined), false);
  assert.equal(isValidResume(123), false);
});
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

Run: `npx tsx --test src/validation.test.ts`
Expected: FAIL — `Cannot find module './validation'`

- [ ] **Step 3: 구현**

`src/validation.ts`:
```ts
// CLI 인자/웹 입력 공통: limit을 1~100 정수로 클램프. 잘못된 값은 기본 50.
export function clampLimit(raw: unknown): number {
  const n = typeof raw === 'number' ? raw : parseInt(String(raw ?? ''), 10);
  if (!Number.isFinite(n) || n <= 0) return 50;
  return Math.min(100, Math.floor(n));
}

// 이력서는 공백 아닌 문자열이어야 한다.
export function isValidResume(resume: unknown): resume is string {
  return typeof resume === 'string' && resume.trim().length > 0;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx tsx --test src/validation.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/validation.ts src/validation.test.ts
git commit -m "feat: 입력 검증 헬퍼(clampLimit, isValidResume) 추가"
```

---

### Task 3: scorer.ts 진행 콜백화

**Files:**
- Modify: `src/scorer.ts:149-167` (`scoreAllJobs`)
- Test: `src/scorer.test.ts`

**Interfaces:**
- Consumes: `WantedJob`, `ScoredJob` (types.ts), 기존 `scoreJob`
- Produces:
  - `scoreAllJobs(jobs, resume, onScored?, scoreOne?)` — 시그니처 확장:
    ```ts
    scoreAllJobs(
      jobs: WantedJob[],
      resume: string,
      onScored?: (e: { index: number; total: number; job: ScoredJob }) => void,
      scoreOne?: (job: WantedJob, resume: string) => Promise<ScoredJob>,
    ): Promise<ScoredJob[]>
    ```
    완료된 공고마다 `onScored({index, total, job})` 호출(index는 1부터). `scoreOne` 미전달 시 실제 `scoreJob` 사용 → 테스트에서 가짜 주입 가능.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/scorer.test.ts`:
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { scoreAllJobs } from './scorer';
import { WantedJob, ScoredJob } from './types';

function makeJob(id: number): WantedJob {
  return {
    id, position: `pos${id}`, companyName: `co${id}`, location: '서울',
    mainTasks: '', requirements: '', preferredPoints: '',
  };
}

test('scoreAllJobs: 공고마다 onScored를 index 1..N, total N으로 호출', async () => {
  const jobs = [makeJob(1), makeJob(2), makeJob(3)];
  // 가짜 채점기: id를 점수로 사용
  const fakeScore = async (job: WantedJob): Promise<ScoredJob> => ({
    ...job, score: job.id * 10, matchPoints: [], gaps: [], summary: '',
  });
  const calls: Array<{ index: number; total: number; score: number }> = [];
  const result = await scoreAllJobs(jobs, '이력서', (e) => {
    calls.push({ index: e.index, total: e.total, score: e.job.score });
  }, fakeScore);

  assert.deepEqual(calls.map((c) => c.index), [1, 2, 3]);
  assert.ok(calls.every((c) => c.total === 3));
  // 결과는 점수 내림차순 정렬
  assert.deepEqual(result.map((j) => j.score), [30, 20, 10]);
});

test('scoreAllJobs: onScored 없이도 동작(하위호환)', async () => {
  const jobs = [makeJob(1)];
  const fakeScore = async (job: WantedJob): Promise<ScoredJob> => ({
    ...job, score: 50, matchPoints: [], gaps: [], summary: '',
  });
  const result = await scoreAllJobs(jobs, '이력서', undefined, fakeScore);
  assert.equal(result.length, 1);
  assert.equal(result[0].score, 50);
});
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

Run: `npx tsx --test src/scorer.test.ts`
Expected: FAIL — 현재 `scoreAllJobs`는 인자 2개만 받고 `onScored`/`scoreOne`을 무시하므로 콜백 미호출로 assertion 실패

- [ ] **Step 3: scoreAllJobs 수정**

`src/scorer.ts`의 `scoreAllJobs`(현재 149~167행)를 아래로 교체:
```ts
export async function scoreAllJobs(
  jobs: WantedJob[],
  resume: string,
  onScored?: (e: { index: number; total: number; job: ScoredJob }) => void,
  scoreOne: (job: WantedJob, resume: string) => Promise<ScoredJob> = scoreJob,
): Promise<ScoredJob[]> {
  const results: ScoredJob[] = [];
  const total = jobs.length;
  let done = 0;

  for (let i = 0; i < jobs.length; i += CONCURRENCY) {
    const batch = jobs.slice(i, i + CONCURRENCY);
    const batchResults = await Promise.all(
      batch.map((job) => scoreOne(job, resume))
    );
    for (const result of batchResults) {
      done++;
      onScored?.({ index: done, total, job: result });
    }
    results.push(...batchResults);
  }

  return results.sort((a, b) => b.score - a.score);
}
```

> 주의: 기존의 per-job `console.log(...)`는 여기서 제거된다. CLI 출력은 Task 5에서 `runPipeline`의 콜백이 재현한다.

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx tsx --test src/scorer.test.ts`
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/scorer.ts src/scorer.test.ts
git commit -m "feat: scoreAllJobs에 진행 콜백·채점기 주입 추가"
```

---

### Task 4: pipeline.ts 공유 오케스트레이션

**Files:**
- Create: `src/pipeline.ts`
- Test: `src/pipeline.test.ts`

**Interfaces:**
- Consumes: `fetchJobsWithDetails` (wanted.ts), `scoreAllJobs` (scorer.ts), `ProgressEvent`/`WantedJob`/`ScoredJob` (types.ts)
- Produces:
  - `PipelineDeps` 인터페이스 `{ fetchJobs, scoreJobs }`
  - `runPipeline(resume, limit, onProgress, deps?): Promise<ScoredJob[]>`
    - onProgress 순서: `status`(수집) → `status`(채점시작) → `scored`×N → `done`
    - 에러 시 `error` 이벤트 emit 후 rethrow

- [ ] **Step 1: 실패하는 테스트 작성**

`src/pipeline.test.ts`:
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { runPipeline, PipelineDeps } from './pipeline';
import { ProgressEvent, WantedJob, ScoredJob } from './types';

function scored(id: number, score: number): ScoredJob {
  return {
    id, position: `p${id}`, companyName: `c${id}`, location: '서울',
    mainTasks: '', requirements: '', preferredPoints: '',
    score, matchPoints: [], gaps: [], summary: '',
  };
}

test('runPipeline: status→status→scored×N→done 순서로 emit', async () => {
  const fakeDeps: PipelineDeps = {
    fetchJobs: async () => [
      { id: 1 } as WantedJob, { id: 2 } as WantedJob,
    ],
    scoreJobs: async (jobs, _resume, onScored) => {
      const out = [scored(1, 70), scored(2, 90)];
      out.forEach((j, i) => onScored?.({ index: i + 1, total: out.length, job: j }));
      return out;
    },
  };
  const events: ProgressEvent[] = [];
  const result = await runPipeline('이력서', 50, (e) => events.push(e), fakeDeps);

  assert.deepEqual(events.map((e) => e.type), ['status', 'status', 'scored', 'scored', 'done']);
  assert.equal((events[4] as any).count, 2);
  assert.equal(result.length, 2);
});

test('runPipeline: 수집 실패 시 error emit 후 throw', async () => {
  const fakeDeps: PipelineDeps = {
    fetchJobs: async () => { throw new Error('API 다운'); },
    scoreJobs: async () => [],
  };
  const events: ProgressEvent[] = [];
  await assert.rejects(
    () => runPipeline('이력서', 50, (e) => events.push(e), fakeDeps),
    /API 다운/
  );
  const last = events.at(-1)!;
  assert.equal(last.type, 'error');
  assert.match((last as any).message, /API 다운/);
});
```

- [ ] **Step 2: 테스트 실행해 실패 확인**

Run: `npx tsx --test src/pipeline.test.ts`
Expected: FAIL — `Cannot find module './pipeline'`

- [ ] **Step 3: 구현**

`src/pipeline.ts`:
```ts
import { fetchJobsWithDetails } from './wanted';
import { scoreAllJobs } from './scorer';
import { WantedJob, ScoredJob, ProgressEvent } from './types';

export interface PipelineDeps {
  fetchJobs: (limit: number) => Promise<WantedJob[]>;
  scoreJobs: (
    jobs: WantedJob[],
    resume: string,
    onScored?: (e: { index: number; total: number; job: ScoredJob }) => void,
  ) => Promise<ScoredJob[]>;
}

const defaultDeps: PipelineDeps = {
  fetchJobs: fetchJobsWithDetails,
  scoreJobs: scoreAllJobs,
};

// 수집→채점을 묶고 단계마다 onProgress를 호출한다. CLI/웹 공용.
export async function runPipeline(
  resume: string,
  limit: number,
  onProgress: (e: ProgressEvent) => void,
  deps: PipelineDeps = defaultDeps,
): Promise<ScoredJob[]> {
  try {
    onProgress({ type: 'status', message: '공고 수집 중...' });
    const jobs = await deps.fetchJobs(limit);

    onProgress({ type: 'status', message: `${jobs.length}개 공고 채점 시작` });
    const result = await deps.scoreJobs(jobs, resume, (e) =>
      onProgress({ type: 'scored', index: e.index, total: e.total, job: e.job }),
    );

    onProgress({ type: 'done', count: result.length });
    return result;
  } catch (err: any) {
    onProgress({ type: 'error', message: String(err?.message ?? err) });
    throw err;
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx tsx --test src/pipeline.test.ts`
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/pipeline.ts src/pipeline.test.ts
git commit -m "feat: 수집·채점 공유 오케스트레이션 runPipeline 추가"
```

---

### Task 5: index.ts(CLI)를 runPipeline로 리팩터

**Files:**
- Modify: `src/index.ts:28-42` (수집·채점 인라인 코드 → runPipeline 호출)

**Interfaces:**
- Consumes: `runPipeline` (pipeline.ts), `clampLimit` (validation.ts), `generateReport` (reporter.ts)
- Produces: 없음(엔트리포인트)

- [ ] **Step 1: import 교체**

`src/index.ts` 상단 import에서 `fetchJobsWithDetails`, `scoreAllJobs` import를 제거하고 추가:
```ts
import { runPipeline } from './pipeline';
import { clampLimit } from './validation';
```
(`generateReport` import는 유지)

- [ ] **Step 2: 수집·채점 블록 교체**

현재 28~42행(주석 `// 3. 공고 수집`부터 `generateReport(...)` 직전까지)을 아래로 교체:
```ts
  // 3. limit 결정 (CLI 인자)
  const limit = clampLimit(process.argv[2] ?? 50);

  // 4. 수집 + 채점 (진행 상황을 콘솔로 출력)
  const scored = await runPipeline(resume, limit, (e) => {
    if (e.type === 'status') {
      console.log(e.message);
    } else if (e.type === 'scored') {
      console.log(
        `  [${String(e.job.score).padStart(3)}점] ${e.job.position} @ ${e.job.companyName}`
      );
    } else if (e.type === 'error') {
      console.error('⚠️ ', e.message);
    }
  });

  // 5. 리포트 생성
  const outPath = path.join(process.cwd(), 'report.html');
  generateReport(scored, outPath);
```

- [ ] **Step 3: 타입 체크 통과 확인**

Run: `npx tsc --noEmit`
Expected: 에러 없음 (exit 0)

- [ ] **Step 4: 전체 테스트 재확인 (회귀 없음)**

Run: `npm test`
Expected: 기존 모든 테스트 PASS

- [ ] **Step 5: 커밋**

```bash
git add src/index.ts
git commit -m "refactor: CLI를 runPipeline 기반으로 전환"
```

---

### Task 6: Express 서버 (단일 실행 잠금 + SSE)

**Files:**
- Create: `src/server.ts` (createApp 팩토리)
- Create: `src/serve.ts` (엔트리포인트)
- Test: `src/server.test.ts`
- Modify: `package.json` (express 의존성 + web 스크립트)

**Interfaces:**
- Consumes: `runPipeline` (pipeline.ts), `clampLimit`/`isValidResume` (validation.ts), `formatSSE` (sse.ts), `ProgressEvent` (types.ts)
- Produces:
  - `ServerDeps` 인터페이스 `{ runPipeline: typeof runPipeline }`
  - `createApp(deps?): express.Express`
    - `POST /run` 본문 `{resume, limit}` → 400(이력서없음)/409(실행중)/200(SSE 스트림)
    - `GET /` 및 정적 파일은 `public/`에서 서빙

- [ ] **Step 1: express 설치**

Run:
```bash
npm install express@^4.19.2 && npm install -D @types/express@^4.17.21
```
Expected: `package.json`의 dependencies에 `express`, devDependencies에 `@types/express` 추가됨

- [ ] **Step 2: 실패하는 테스트 작성**

`src/server.test.ts`:
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import type { AddressInfo } from 'node:net';
import { createApp, ServerDeps } from './server';

// 테스트용: 서버를 임시 포트로 띄우고 base URL 반환
async function listen(deps: ServerDeps): Promise<{ url: string; close: () => Promise<void> }> {
  const server = createApp(deps).listen(0);
  await new Promise((r) => server.once('listening', r));
  const port = (server.address() as AddressInfo).port;
  return {
    url: `http://localhost:${port}`,
    close: () => new Promise((r) => server.close(() => r(undefined))),
  };
}

test('POST /run: 이력서 없으면 400', async () => {
  const { url, close } = await listen({ runPipeline: (async () => []) as any });
  try {
    const res = await fetch(`${url}/run`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ limit: 10 }),
    });
    assert.equal(res.status, 400);
  } finally {
    await close();
  }
});

test('POST /run: 실행 중이면 두 번째 요청 409, 끝나면 다시 200', async () => {
  let releaseGate!: () => void;
  const gate = new Promise<void>((r) => { releaseGate = r; });
  const fakeRun = (async (_resume: string, _limit: number, onProgress: any) => {
    onProgress({ type: 'status', message: 'start' });
    await gate;             // 잠금을 잡은 채 대기
    onProgress({ type: 'done', count: 0 });
    return [];
  }) as any;

  const { url, close } = await listen({ runPipeline: fakeRun });
  try {
    // 요청1: 시작(헤더 수신되면 fetch resolve, 본문 스트림은 열린 채 유지)
    const req1 = await fetch(`${url}/run`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5 }),
    });
    assert.equal(req1.status, 200);

    // 요청2: 잠금 중이라 409
    const req2 = await fetch(`${url}/run`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5 }),
    });
    assert.equal(req2.status, 409);

    // 잠금 해제 후 요청1 스트림 드레인
    releaseGate();
    await req1.text();

    // 이제 다시 200
    const req3 = await fetch(`${url}/run`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5 }),
    });
    assert.equal(req3.status, 200);
    await req3.text();
  } finally {
    await close();
  }
});
```

- [ ] **Step 3: 테스트 실행해 실패 확인**

Run: `npx tsx --test src/server.test.ts`
Expected: FAIL — `Cannot find module './server'`

- [ ] **Step 4: server.ts 구현**

`src/server.ts`:
```ts
import express from 'express';
import * as path from 'path';
import { runPipeline } from './pipeline';
import { clampLimit, isValidResume } from './validation';
import { formatSSE } from './sse';
import { ProgressEvent } from './types';

export interface ServerDeps {
  runPipeline: typeof runPipeline;
}

const defaultDeps: ServerDeps = { runPipeline };

export function createApp(deps: ServerDeps = defaultDeps): express.Express {
  const app = express();
  app.use(express.json({ limit: '1mb' }));
  app.use(express.static(path.join(process.cwd(), 'public')));

  // GPU 1개 → 동시에 하나만 실행. 모듈 스코프 잠금.
  let running = false;

  app.post('/run', async (req, res) => {
    const body = req.body ?? {};
    if (!isValidResume(body.resume)) {
      res.status(400).json({ error: '이력서를 입력하세요.' });
      return;
    }
    if (running) {
      res.status(409).json({ error: '이미 실행 중입니다.' });
      return;
    }

    running = true;
    res.writeHead(200, {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      Connection: 'keep-alive',
    });
    const send = (e: ProgressEvent) => res.write(formatSSE(e));

    try {
      await deps.runPipeline(body.resume, clampLimit(body.limit), send);
    } catch {
      // error 이벤트는 runPipeline이 이미 send 했으므로 여기선 스트림만 닫는다.
    } finally {
      running = false;
      res.end();
    }
  });

  return app;
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npx tsx --test src/server.test.ts`
Expected: PASS (2 tests)

- [ ] **Step 6: 엔트리포인트 + 스크립트 추가**

`src/serve.ts`:
```ts
import { createApp } from './server';

const port = Number(process.env.PORT ?? 3000);
createApp().listen(port, () => {
  console.log(`🌐 웹 서버 실행: http://localhost:${port}`);
  console.log('   브라우저에서 위 주소를 열어 사용하세요. (Ollama 서버가 켜져 있어야 합니다)');
});
```

`package.json`의 `scripts`에 추가:
```json
    "web": "tsx src/serve.ts"
```

- [ ] **Step 7: 커밋**

```bash
git add src/server.ts src/serve.ts src/server.test.ts package.json package-lock.json
git commit -m "feat: Express 서버(단일 실행 잠금 + SSE 스트리밍) 추가"
```

---

### Task 7: 프론트엔드 (원티드풍 GUI)

**Files:**
- Create: `public/index.html`
- Create: `public/style.css`
- Create: `public/app.js`

**Interfaces:**
- Consumes: `POST /run` (server.ts) — JSON `{resume, limit}` 전송, SSE 응답 스트림 수신
- Produces: 없음(정적 자산)

> 이 태스크는 자동 테스트 대신 수동 확인으로 검증한다(브라우저 DOM). 코드는 전부 완성된 형태로 제공한다.

- [ ] **Step 1: index.html 작성**

`public/index.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Wanted Job Matcher</title>
  <link rel="preconnect" href="https://cdn.jsdelivr.net" />
  <link rel="stylesheet"
    href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <header class="topbar">
    <span class="brand">wanted<span class="brand-accent">match</span></span>
    <span class="topbar-sub">내 이력서로 채용 공고 자동 채점</span>
  </header>

  <main class="container">
    <section class="panel">
      <label class="field-label" for="resume">이력서</label>
      <textarea id="resume" class="resume-input" rows="8"
        placeholder="이력서 전문을 붙여넣으세요."></textarea>

      <div class="run-row">
        <label class="field-label inline" for="limit">공고 수</label>
        <input id="limit" class="limit-input" type="number" min="1" max="100" value="50" />
        <button id="runBtn" class="run-btn">분석 시작</button>
      </div>

      <div id="statusBar" class="status-bar" hidden>
        <span id="statusText" class="status-text"></span>
        <div class="progress-track"><div id="progressFill" class="progress-fill"></div></div>
      </div>
      <div id="errorBar" class="error-bar" hidden></div>
    </section>

    <section id="results" class="results"></section>
  </main>

  <script src="/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: style.css 작성 (원티드풍 라이트 테마)**

`public/style.css`:
```css
:root {
  --primary: #3366ff;
  --primary-dark: #1f4fe0;
  --bg: #f4f5f7;
  --surface: #ffffff;
  --border: #e8eaef;
  --text: #1a1a1a;
  --muted: #767986;
}
* { box-sizing: border-box; margin: 0; padding: 0; }
body {
  font-family: 'Pretendard', -apple-system, system-ui, sans-serif;
  background: var(--bg);
  color: var(--text);
  min-height: 100vh;
}
.topbar {
  display: flex; align-items: baseline; gap: 12px;
  padding: 18px 24px; background: var(--surface);
  border-bottom: 1px solid var(--border);
}
.brand { font-size: 1.25rem; font-weight: 800; letter-spacing: -0.02em; }
.brand-accent { color: var(--primary); }
.topbar-sub { font-size: 0.8rem; color: var(--muted); }

.container { max-width: 820px; margin: 0 auto; padding: 24px 16px 64px; }
.panel {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 16px; padding: 24px; margin-bottom: 24px;
}
.field-label { display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 8px; }
.field-label.inline { display: inline; margin: 0; }
.resume-input {
  width: 100%; border: 1px solid var(--border); border-radius: 10px;
  padding: 14px; font-size: 0.9rem; font-family: inherit; resize: vertical;
  line-height: 1.6;
}
.resume-input:focus { outline: none; border-color: var(--primary); }

.run-row { display: flex; align-items: center; gap: 12px; margin-top: 16px; }
.limit-input {
  width: 80px; border: 1px solid var(--border); border-radius: 8px;
  padding: 9px 10px; font-size: 0.9rem; font-family: inherit;
}
.limit-input:focus { outline: none; border-color: var(--primary); }
.run-btn {
  margin-left: auto; background: var(--primary); color: #fff; border: none;
  border-radius: 10px; padding: 11px 22px; font-size: 0.92rem; font-weight: 700;
  cursor: pointer; transition: background 0.15s;
}
.run-btn:hover:not(:disabled) { background: var(--primary-dark); }
.run-btn:disabled { background: #b7c4f0; cursor: not-allowed; }

.status-bar { margin-top: 18px; }
.status-text { font-size: 0.82rem; color: var(--muted); }
.progress-track {
  margin-top: 8px; height: 6px; background: var(--border);
  border-radius: 3px; overflow: hidden;
}
.progress-fill { height: 100%; width: 0; background: var(--primary); transition: width 0.3s; }

.error-bar {
  margin-top: 16px; padding: 12px 14px; border-radius: 10px;
  background: #fdecec; color: #c0392b; font-size: 0.85rem;
}

.results { display: flex; flex-direction: column; gap: 12px; }
.card {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 14px; padding: 18px 20px;
  opacity: 0; transform: translateY(10px); animation: fadeUp 0.35s forwards;
}
@keyframes fadeUp { to { opacity: 1; transform: translateY(0); } }
.card-head { display: flex; align-items: center; gap: 10px; margin-bottom: 8px; }
.badge {
  font-size: 0.68rem; font-weight: 700; padding: 3px 9px; border-radius: 20px;
}
.card-loc { font-size: 0.76rem; color: var(--muted); }
.card-title { font-size: 1.02rem; font-weight: 700; line-height: 1.3; }
.card-title a { color: var(--text); text-decoration: none; }
.card-title a:hover { color: var(--primary); }
.card-company { font-size: 0.82rem; color: var(--muted); margin: 2px 0 8px; }
.card-summary { font-size: 0.86rem; color: #444; line-height: 1.55; }
.card-foot { display: flex; align-items: center; gap: 14px; margin-top: 12px; }
.score-pill {
  font-weight: 800; font-size: 1.05rem; color: var(--primary);
  min-width: 48px;
}
.card-tags { display: flex; flex-wrap: wrap; gap: 6px; }
.tag {
  font-size: 0.72rem; padding: 3px 8px; border-radius: 6px;
  background: #eef2ff; color: var(--primary-dark);
}
.tag.gap { background: #fff4e5; color: #b9770e; }

@media (max-width: 560px) {
  .run-row { flex-wrap: wrap; }
  .run-btn { margin-left: 0; width: 100%; }
}
```

- [ ] **Step 3: app.js 작성 (fetch 스트리밍 + 라이브 렌더)**

`public/app.js`:
```js
const $ = (id) => document.getElementById(id);

function gradeBadge(score) {
  if (score >= 80) return { label: '강력 추천', color: '#065f46', bg: '#d1fae5' };
  if (score >= 65) return { label: '검토 추천', color: '#92400e', bg: '#fef3c7' };
  if (score >= 50) return { label: '참고 가능', color: '#1e3a8a', bg: '#dbeafe' };
  return { label: '낮은 핏', color: '#6b7280', bg: '#f3f4f6' };
}
function esc(s) {
  return String(s ?? '').replace(/[&<>"']/g, (c) =>
    ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
}

function addCard(job) {
  const g = gradeBadge(job.score);
  const url = `https://www.wanted.co.kr/wd/${job.id}`;
  const tags = (job.matchPoints || []).map((p) => `<span class="tag">${esc(p)}</span>`).join('');
  const gaps = (job.gaps || []).map((p) => `<span class="tag gap">${esc(p)}</span>`).join('');
  const el = document.createElement('article');
  el.className = 'card';
  el.dataset.score = job.score;
  el.innerHTML = `
    <div class="card-head">
      <span class="badge" style="background:${g.bg};color:${g.color}">${g.label}</span>
      <span class="card-loc">${esc(job.location)}</span>
    </div>
    <h2 class="card-title"><a href="${url}" target="_blank">${esc(job.position)}</a></h2>
    <p class="card-company">${esc(job.companyName)}</p>
    <p class="card-summary">${esc(job.summary)}</p>
    <div class="card-foot"><span class="score-pill">${job.score}점</span>
      <div class="card-tags">${tags}${gaps}</div></div>`;
  $('results').appendChild(el);
}

function sortCards() {
  const cards = [...document.querySelectorAll('.card')];
  cards.sort((a, b) => Number(b.dataset.score) - Number(a.dataset.score));
  const root = $('results');
  cards.forEach((c) => root.appendChild(c));
}

function setStatus(text) { $('statusText').textContent = text; }
function setProgress(index, total) {
  $('progressFill').style.width = total ? `${(index / total) * 100}%` : '0';
}
function showError(msg) {
  const bar = $('errorBar');
  bar.textContent = '⚠️ ' + msg;
  bar.hidden = false;
}
function setRunning(on) {
  $('runBtn').disabled = on;
  $('runBtn').textContent = on ? '분석 중...' : '분석 시작';
}

function handleChunk(chunk) {
  const dataLine = chunk.split('\n').find((l) => l.startsWith('data: '));
  if (!dataLine) return;
  const e = JSON.parse(dataLine.slice(6));
  if (e.type === 'status') setStatus(e.message);
  else if (e.type === 'scored') { addCard(e.job); setStatus(`채점 중 ${e.index}/${e.total}`); setProgress(e.index, e.total); }
  else if (e.type === 'done') { sortCards(); setStatus(`완료 — ${e.count}개 공고`); setProgress(1, 1); }
  else if (e.type === 'error') showError(e.message);
}

async function run() {
  const resume = $('resume').value.trim();
  if (!resume) { showError('이력서를 입력하세요.'); return; }

  $('errorBar').hidden = true;
  $('results').innerHTML = '';
  $('statusBar').hidden = false;
  setProgress(0, 1);
  setRunning(true);

  try {
    const res = await fetch('/run', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume, limit: Number($('limit').value) }),
    });
    if (res.status === 409) { showError('이미 실행 중입니다. 잠시 후 다시 시도하세요.'); return; }
    if (res.status === 400) { showError((await res.json()).error); return; }

    const reader = res.body.getReader();
    const decoder = new TextDecoder();
    let buf = '';
    for (;;) {
      const { done, value } = await reader.read();
      if (done) break;
      buf += decoder.decode(value, { stream: true });
      let idx;
      while ((idx = buf.indexOf('\n\n')) !== -1) {
        handleChunk(buf.slice(0, idx));
        buf = buf.slice(idx + 2);
      }
    }
  } catch (err) {
    showError(String(err?.message ?? err));
  } finally {
    setRunning(false);
  }
}

$('runBtn').addEventListener('click', run);
```

- [ ] **Step 4: 수동 확인 — 서버 기동 & 화면**

Run: `npm run web`
그리고 브라우저에서 `http://localhost:3000` 접속.
확인 항목:
- 원티드풍 라이트 화면이 뜨고 이력서 입력란·공고 수·"분석 시작" 버튼이 보인다.
- 이력서 비운 채 클릭 → "이력서를 입력하세요" 에러 배너.
- (Ollama 실행 중이라면) 이력서 붙여넣고 공고 수 5로 클릭 → 상태줄이 "공고 수집 중" → "채점 중 n/5"로 갱신되고 카드가 한 장씩 추가된 뒤 점수순 정렬된다.
- 분석 중 버튼이 비활성화되고 끝나면 다시 활성화된다.

> Ollama가 없으면 카드 단계 대신 에러 배너("Ollama 서버에 연결할 수 없습니다")가 뜨면 정상.

- [ ] **Step 5: 커밋**

```bash
git add public/index.html public/style.css public/app.js
git commit -m "feat: 원티드풍 웹 GUI(이력서 입력·실시간 진행률·결과 카드) 추가"
```

---

### Task 8: README 사용법 갱신

**Files:**
- Modify: `README.md` (웹 사용법 섹션 추가)

**Interfaces:**
- Consumes: 없음
- Produces: 없음

- [ ] **Step 1: 웹 실행 안내 추가**

`README.md`에서 CLI 실행 안내 근처에 "웹으로 쓰기" 섹션을 추가:
```markdown
## 웹으로 쓰기 (GUI)

CLI 대신 브라우저에서 쓰고 싶으면:

\`\`\`bash
npm run web
\`\`\`

그리고 브라우저에서 http://localhost:3000 접속.
이력서를 붙여넣고 공고 수를 정한 뒤 "분석 시작"을 누르면, 공고가 한 건씩
채점되며 실시간으로 카드가 채워집니다. (Ollama 서버가 켜져 있어야 합니다.)

> GPU가 1개라 한 번에 한 분석만 돌아갑니다. 실행 중 다시 누르면 "이미 실행 중"
> 안내가 뜹니다. 포트를 바꾸려면 \`PORT=4000 npm run web\`.
```

- [ ] **Step 2: 커밋**

```bash
git add README.md
git commit -m "docs: 웹 GUI 사용법 README 추가"
```

---

## Self-Review

**Spec coverage 점검:**
- 실시간 진행률(SSE) → Task 1(formatSSE), Task 4(이벤트 emit), Task 6(스트리밍), Task 7(수신/렌더) ✓
- 이력서 브라우저 붙여넣기 → Task 7(textarea) + Task 6(POST 본문) ✓
- Express 스택 → Task 6 ✓
- 결과 라이브 카드 렌더 → Task 7(addCard/sortCards) ✓
- 단일 실행 잠금 → Task 6(running 플래그, 409, finally 해제) + 테스트 ✓
- fetch POST + 스트림(EventSource 회피) → Task 7(app.js getReader) ✓
- 원티드풍 CSS → Task 7(style.css) ✓
- 에러 처리(Ollama 미실행/이력서없음/범위/0건/개별실패/연결끊김) → Task 6(검증·finally) + Task 7(showError) + 기존 scorer 메시지 재사용 ✓
- 테스트(검증·잠금·SSE포맷·pipeline콜백) → Task 1·2·3·4·6 ✓
- 기존 동작 무회귀(wanted/reporter 무수정, scorer 하위호환) → Task 3 하위호환 테스트 + Task 5 회귀 테스트 ✓

**Placeholder scan:** 모든 코드 스텝에 실제 코드 포함, "TODO/적절히 처리" 류 없음 ✓

**Type consistency:** `ProgressEvent`(Task 1) ↔ `formatSSE`(1) ↔ `onScored {index,total,job}`(3) ↔ `runPipeline`/`PipelineDeps`(4) ↔ `createApp`/`ServerDeps`(6) ↔ 프론트 `handleChunk`(7) 전부 동일 필드명 사용. `clampLimit`/`isValidResume`(2)는 index(5)·server(6)에서 동일 시그니처로 소비 ✓
