# 조각 D — 자연어 쿼리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 웹 사용자가 자유 텍스트("자바 잘하는 신입 백엔드")와/또는 직군 칩 다중 선택으로 수집할 직군·키워드를 지정하게 한다.

**Architecture:** 자연어는 gemma가 `{roles[], keywords[]}`로 추출하고 코드 사전(`jobTags.ts`)이 role명→원티드 태그 ID로 매핑한다. 칩은 LLM 없이 태그 ID로 직결된다. 둘을 `SearchSpec{tagIds,keywords}`로 병합해 `runPipeline`→`fetchJobsWithDetails`까지 스레딩한다. 태그는 원티드 API `tag_type_ids` 필터, 키워드는 API `query` 파라미터(best-effort)+수집 후 텍스트 필터(보장)로 적용한다. CLI는 기본 `SearchSpec`(기존 4직군)으로 불변.

**Tech Stack:** TypeScript, tsx, node:test, Express, ollama(gemma4:12b), 원티드 내부 API.

## Global Constraints

- 커밋 prefix는 영어 conventional(`feat:`/`fix:`/`refactor:`/`test:`/`docs:`), 메시지 본문 한국어 허용.
- 커밋 끝에 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 추가. **푸시는 금지**(사용자 명시 요청 시에만).
- gemma 호출은 반드시 `think:false`(안 끄면 content 비어 JSON 파싱 전부 실패 → 전 항목 폴백).
- 테스트 실행: `npm test`(= `tsx --test "src/**/*.test.ts"`). 단일 파일: `npx tsx --test src/X.test.ts`.
- 테스트는 네트워크/Ollama에 의존하지 않는다 — 순수 함수 추출 + 의존성 주입으로 오프라인 검증.
- CLI(`src/index.ts`) 동작 불변: 기존 4직군 신입 수집이 그대로 유지돼야 한다.
- 기존 25개 테스트가 계속 통과해야 한다(회귀 금지).

## 설계 메모 (빈 태그 정책)

- **상황 A** — 자유입력 없음 + 칩 없음 → 실행 차단(400). `isValidSearch`로 거름.
- **상황 B** — 자유입력은 있으나 LLM이 아는 직군을 못 뽑고, 칩도 없어 **병합 태그가 0개** →
  실행 차단(400) + 안내 "직군을 인식하지 못했어요. 직군 칩을 선택하거나 직군명을 포함해 다시 입력하세요."
- 결과적으로 `runPipeline`/`wanted.ts`에 도달하는 `SearchSpec.tagIds`는 **항상 1개 이상**(서버가 보장).
  태그 없이 원티드 `/jobs`를 호출(전체 공고 수만 건 수집)하는 경로는 존재하지 않는다.
- 키워드는 항상 선택/인식된 태그 범위 안에서만 좁히는 역할(서버사이드 `query` + 수집 후 필터).

## File Structure

| 파일 | 책임 | 신규/변경 |
|---|---|---|
| `src/types.ts` | `SearchSpec` 타입 추가 | 변경 |
| `src/jobTags.ts` | 직군 사전(단일 진실원천): 라벨/별칭↔태그ID, 기본값, 조회 헬퍼 | 신규 |
| `src/jsonParse.ts` | `parseJsonLenient` 공용 추출(scorer/queryParser 공유) | 신규 |
| `src/scorer.ts` | `parseJsonLenient`를 `jsonParse`에서 import로 교체 | 변경 |
| `src/queryParser.ts` | gemma로 자유텍스트→`SearchSpec` 추출+매핑(+폴백) | 신규 |
| `src/wanted.ts` | `SearchSpec` 기반 태그/키워드 파라미터·수집 후 키워드 필터 | 변경 |
| `src/pipeline.ts` | `runPipeline`/`PipelineDeps`에 `searchSpec` 스레딩 | 변경 |
| `src/validation.ts` | `sanitizeTagIds`, `isValidSearch` 추가 | 변경 |
| `src/server.ts` | `/run`에 searchSpec 구성, `GET /tags`, `parseQuery` 의존성 | 변경 |
| `public/index.html` | 쿼리 입력칸 + 칩 컨테이너 | 변경 |
| `public/app.js` | `/tags` 렌더·칩 토글·payload·빈입력 가드 | 변경 |
| `docs/superpowers/specs/2026-06-23-nl-query-api-findings.md` | API 프로브 결과 기록 | 신규(Task 1) |

---

### Task 1: 원티드 API 라이브 프로브 (검증·발견) — ✅ 완료 (2026-06-23)

> 결과는 `docs/superpowers/specs/2026-06-23-nl-query-api-findings.md`. 확정값:
> 프론트엔드=669, AI/ML=1634(코드 839는 깨짐), DevOps=674(코드 940은 깨짐), `query` 서버사이드 narrowing 동작 확인.
> 이 값들은 Task 2 사전·Task 5 설계에 이미 반영됨. findings 문서 커밋은 사용자 승인 시.
>
> ⚠️ 네트워크 필요. **서브에이전트에 위임하지 말고 메인 세션에서** 수행(재실행 시).

**Files:**
- Create: `docs/superpowers/specs/2026-06-23-nl-query-api-findings.md`

**Interfaces:**
- Produces: `FRONTEND_TAG_ID`(정수) — Task 2의 사전에 들어갈 프론트엔드 직군 태그 ID. `query` 파라미터 서버사이드 narrowing 여부(boolean) — Task 5의 주석/기대치.

- [ ] **Step 1: 기존 4직군 호출이 여전히 동작하는지 확인(베이스라인)**

Run:
```bash
curl -s 'https://www.wanted.co.kr/api/v4/jobs?country=kr&job_sort=job.latest_order&years=0&limit=20&tag_type_ids=872&tag_type_ids=839&tag_type_ids=940&tag_type_ids=655' \
  -H 'User-Agent: Mozilla/5.0' -H 'Referer: https://www.wanted.co.kr/jobs' | python3 -c 'import sys,json; d=json.load(sys.stdin); print(len(d.get("data",[])))'
```
Expected: 1 이상(공고 건수). 0이면 헤더/엔드포인트 변경 — 중단하고 사용자에게 보고.

- [ ] **Step 2: 프론트엔드 직군 태그 ID 발견**

브라우저에서 https://www.wanted.co.kr/wdlist 열기 → DevTools Network 탭 → 직군 필터에서 "프론트엔드 개발자" 선택 → `/api/v4/jobs?...` 요청 URL의 `tag_type_ids=NNN` 값을 읽는다.
(메인 세션이면 사용자에게 `!` 로 위 절차를 요청하거나, 후보 ID로 아래 Step 3 검증)

- [ ] **Step 3: 발견한 프론트엔드 ID 검증(공고 반환 확인)**

Run(발견값을 NNN에 대입):
```bash
curl -s 'https://www.wanted.co.kr/api/v4/jobs?country=kr&job_sort=job.latest_order&years=0&limit=20&tag_type_ids=NNN' \
  -H 'User-Agent: Mozilla/5.0' -H 'Referer: https://www.wanted.co.kr/jobs' | python3 -c 'import sys,json; d=json.load(sys.stdin); [print(j.get("position")) for j in d.get("data",[])[:10]]'
```
Expected: 프론트엔드 관련 포지션명이 출력. 무관하면 다른 ID 재시도.

- [ ] **Step 4: `query` 파라미터 서버사이드 narrowing 여부 확인**

Run:
```bash
curl -s 'https://www.wanted.co.kr/api/v4/jobs?country=kr&job_sort=job.latest_order&years=0&limit=20&tag_type_ids=872&query=spring' \
  -H 'User-Agent: Mozilla/5.0' -H 'Referer: https://www.wanted.co.kr/jobs' | python3 -c 'import sys,json; d=json.load(sys.stdin); print(len(d.get("data",[])))'
```
`query` 유/무 결과를 비교해 narrowing이 실제로 일어나는지 관찰. (어느 쪽이든 Task 5의 수집 후 키워드 필터가 정확성을 보장하므로 진행에는 영향 없음 — 정보 기록용.)

- [ ] **Step 5: findings 문서 작성 + 커밋**

`docs/superpowers/specs/2026-06-23-nl-query-api-findings.md`에 기록: 프론트엔드 태그 ID(확정값), `query` 파라미터 동작 여부, 베이스라인 건수.
```bash
git add docs/superpowers/specs/2026-06-23-nl-query-api-findings.md
git commit -m "docs: 원티드 API 프로브 결과(프론트엔드 태그 ID, query 동작)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: SearchSpec 타입 + jobTags 직군 사전

**Files:**
- Modify: `src/types.ts`
- Create: `src/jobTags.ts`
- Test: `src/jobTags.test.ts`

**Interfaces:**
- Consumes: Task 1의 프론트엔드 태그 ID(확정 정수).
- Produces:
  - `SearchSpec { tagIds: number[]; keywords: string[] }` (types.ts)
  - `JOB_TAGS: JobTag[]`, `DEFAULT_TAG_IDS: number[]`, `DEFAULT_SEARCH: SearchSpec`
  - `nameToTagId(name: string): number | undefined`
  - `isKnownTagId(id: number): boolean`
  - `tagOptions(): { id: number; label: string }[]`

- [ ] **Step 1: SearchSpec 타입 추가**

`src/types.ts` 끝에 추가:
```ts
export interface SearchSpec {
  tagIds: number[];
  keywords: string[];
}
```

- [ ] **Step 2: 실패 테스트 작성** — `src/jobTags.test.ts`

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { nameToTagId, isKnownTagId, tagOptions, DEFAULT_TAG_IDS } from './jobTags';

test('nameToTagId: 라벨/별칭/대소문자/공백 매핑', () => {
  assert.equal(nameToTagId('백엔드'), 872);
  assert.equal(nameToTagId('Backend'), 872);
  assert.equal(nameToTagId('  서버  '), 872);
  assert.equal(nameToTagId('데이터엔지니어'), 655);
});

test('nameToTagId: 미지의 직군은 undefined', () => {
  assert.equal(nameToTagId('마케팅'), undefined);
});

test('isKnownTagId: 사전에 있는 ID만 true', () => {
  assert.equal(isKnownTagId(872), true);
  assert.equal(isKnownTagId(99999), false);
});

test('tagOptions: 라벨+id 목록 반환, 기본 4직군 포함', () => {
  const opts = tagOptions();
  assert.ok(opts.every((o) => typeof o.id === 'number' && typeof o.label === 'string'));
  for (const id of DEFAULT_TAG_IDS) assert.ok(opts.some((o) => o.id === id));
});
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npx tsx --test src/jobTags.test.ts`
Expected: FAIL (`Cannot find module './jobTags'`)

- [ ] **Step 4: jobTags.ts 구현** — `src/jobTags.ts`

(ID는 Task 1 메타 `/api/v4/tags`(개발 그룹 518)로 확정. 839/940은 깨진 레거시라 1634/674로 정정.)
```ts
import { SearchSpec } from './types';

export interface JobTag {
  id: number;
  label: string;
  aliases: string[];
}

// 직군 사전 — 단일 진실원천. 프론트 칩·LLM 매핑 모두 여기서 파생.
export const JOB_TAGS: JobTag[] = [
  { id: 872, label: '백엔드', aliases: ['백엔드', '백앤드', '서버', 'server', 'backend'] },
  { id: 1634, label: 'AI/머신러닝', aliases: ['ai', '에이아이', '머신러닝', 'ml', '인공지능', '딥러닝', 'machine learning'] },
  { id: 674, label: 'DevOps/인프라', aliases: ['devops', '데브옵스', '인프라', 'infra', 'sre'] },
  { id: 655, label: '데이터엔지니어', aliases: ['데이터엔지니어', '데이터 엔지니어', '데이터', 'data engineer'] },
  { id: 669, label: '프론트엔드', aliases: ['프론트엔드', '프론트', 'frontend', 'front-end', 'fe', '웹퍼블리셔'] },
];

export const DEFAULT_TAG_IDS: number[] = [872, 1634, 674, 655];

export const DEFAULT_SEARCH: SearchSpec = { tagIds: DEFAULT_TAG_IDS, keywords: [] };

export function nameToTagId(name: string): number | undefined {
  const n = name.trim().toLowerCase();
  if (!n) return undefined;
  const hit = JOB_TAGS.find(
    (t) => t.label.toLowerCase() === n || t.aliases.some((a) => a.toLowerCase() === n),
  );
  return hit?.id;
}

export function isKnownTagId(id: number): boolean {
  return JOB_TAGS.some((t) => t.id === id);
}

export function tagOptions(): { id: number; label: string }[] {
  return JOB_TAGS.map(({ id, label }) => ({ id, label }));
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npx tsx --test src/jobTags.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 6: 커밋**

```bash
git add src/types.ts src/jobTags.ts src/jobTags.test.ts
git commit -m "feat: SearchSpec 타입과 직군 사전(jobTags) 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: parseJsonLenient 공용 모듈 추출 (refactor)

**Files:**
- Create: `src/jsonParse.ts`
- Modify: `src/scorer.ts:78-92`(함수 제거), `src/scorer.ts:1`(import 추가)
- Test: `src/jsonParse.test.ts`

**Interfaces:**
- Produces: `parseJsonLenient(raw: string): any` (코드펜스 제거 + `{…}` 추출, 실패 시 throw)

- [ ] **Step 1: 실패 테스트 작성** — `src/jsonParse.test.ts`

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { parseJsonLenient } from './jsonParse';

test('parseJsonLenient: 순수 JSON 파싱', () => {
  assert.deepEqual(parseJsonLenient('{"a":1}'), { a: 1 });
});

test('parseJsonLenient: 코드펜스 제거', () => {
  assert.deepEqual(parseJsonLenient('```json\n{"a":2}\n```'), { a: 2 });
});

test('parseJsonLenient: 앞뒤 잡텍스트 사이 {…} 추출', () => {
  assert.deepEqual(parseJsonLenient('결과: {"a":3} 끝'), { a: 3 });
});

test('parseJsonLenient: JSON 없으면 throw', () => {
  assert.throws(() => parseJsonLenient('no json here'));
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/jsonParse.test.ts`
Expected: FAIL (`Cannot find module './jsonParse'`)

- [ ] **Step 3: jsonParse.ts 구현** — `src/scorer.ts:79-92`의 함수를 그대로 옮긴다

```ts
// format 스키마가 깨진 경우를 대비한 관대한 파서 (코드펜스 제거 + {…} 추출)
export function parseJsonLenient(raw: string): any {
  let text = raw.replace(/```json\s*/gi, '').replace(/```/g, '').trim();
  try {
    return JSON.parse(text);
  } catch {
    /* fallthrough */
  }
  const start = text.indexOf('{');
  const end = text.lastIndexOf('}');
  if (start !== -1 && end !== -1 && end > start) {
    return JSON.parse(text.slice(start, end + 1));
  }
  throw new Error('No JSON object found');
}
```

- [ ] **Step 4: scorer.ts에서 중복 함수 제거 후 import로 교체**

`src/scorer.ts:79-92`의 `parseJsonLenient` 함수 정의 삭제. `src/scorer.ts:2`(types import) 아래에 추가:
```ts
import { parseJsonLenient } from './jsonParse';
```

- [ ] **Step 5: 테스트 통과 확인(신규 + scorer 회귀)**

Run: `npx tsx --test src/jsonParse.test.ts src/scorer.test.ts`
Expected: PASS (jsonParse 4 + 기존 scorer 테스트 전부)

- [ ] **Step 6: 커밋**

```bash
git add src/jsonParse.ts src/jsonParse.test.ts src/scorer.ts
git commit -m "refactor: parseJsonLenient를 jsonParse 공용 모듈로 추출

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: queryParser — 자유텍스트 → SearchSpec

**Files:**
- Create: `src/queryParser.ts`
- Test: `src/queryParser.test.ts`

**Interfaces:**
- Consumes: `nameToTagId`(jobTags), `parseJsonLenient`(jsonParse), `SearchSpec`(types).
- Produces: `parseQuery(text: string, chat?: ChatFn): Promise<SearchSpec>`
  - `type ChatFn = (req: any) => Promise<{ message: { content: string } }>`
  - 성공: `{ tagIds:(매핑된 고유 ID), keywords:(문자열 배열) }`
  - 실패/예외: `{ tagIds: [], keywords: [text.trim()] }` (원문을 키워드로 폴백)

- [ ] **Step 1: 실패 테스트 작성** — `src/queryParser.test.ts`

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { parseQuery } from './queryParser';

const fakeChat = (content: string) => async () => ({ message: { content } });

test('parseQuery: roles를 태그ID로 매핑, keywords 보존', async () => {
  const spec = await parseQuery('자바 잘하는 신입 백엔드', fakeChat('{"roles":["백엔드"],"keywords":["자바"]}'));
  assert.deepEqual(spec.tagIds, [872]);
  assert.deepEqual(spec.keywords, ['자바']);
});

test('parseQuery: 미지의 role은 버리고 키워드만 남김', async () => {
  const spec = await parseQuery('마케팅 자바', fakeChat('{"roles":["마케팅"],"keywords":["자바"]}'));
  assert.deepEqual(spec.tagIds, []);
  assert.deepEqual(spec.keywords, ['자바']);
});

test('parseQuery: 중복 태그 제거', async () => {
  const spec = await parseQuery('서버 백엔드', fakeChat('{"roles":["서버","백엔드"],"keywords":[]}'));
  assert.deepEqual(spec.tagIds, [872]);
});

test('parseQuery: JSON 깨지면 원문을 키워드로 폴백', async () => {
  const spec = await parseQuery('데이터 엔지니어 원해', fakeChat('쓰레기응답'));
  assert.deepEqual(spec.tagIds, []);
  assert.deepEqual(spec.keywords, ['데이터 엔지니어 원해']);
});

test('parseQuery: chat 예외도 폴백', async () => {
  const throwing = async () => { throw new Error('ollama down'); };
  const spec = await parseQuery('백엔드', throwing as any);
  assert.deepEqual(spec.keywords, ['백엔드']);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/queryParser.test.ts`
Expected: FAIL (`Cannot find module './queryParser'`)

- [ ] **Step 3: queryParser.ts 구현**

```ts
import ollama, { Ollama } from 'ollama';
import { SearchSpec } from './types';
import { nameToTagId, JOB_TAGS } from './jobTags';
import { parseJsonLenient } from './jsonParse';

const client = process.env.OLLAMA_HOST ? new Ollama({ host: process.env.OLLAMA_HOST }) : ollama;
const MODEL = process.env.OLLAMA_MODEL ?? 'gemma4:12b';

export type ChatFn = (req: any) => Promise<{ message: { content: string } }>;
const defaultChat: ChatFn = (req) => client.chat({ ...req, stream: false }) as any;

const SCHEMA = {
  type: 'object',
  properties: {
    roles: { type: 'array', items: { type: 'string' } },
    keywords: { type: 'array', items: { type: 'string' } },
  },
  required: ['roles', 'keywords'],
} as const;

function buildPrompt(text: string): string {
  const labels = JOB_TAGS.map((t) => t.label).join(', ');
  return `사용자의 구직 요청에서 직군(role)과 기술 키워드를 추출하세요.

요청: "${text}"

직군은 반드시 다음 목록 중에서만 고르세요(해당 없으면 roles는 빈 배열):
${labels}

키워드는 프로그래밍 언어/프레임워크/도구 등 구체 기술명만(예: 자바, Spring, Kubernetes). 직군명은 키워드에 넣지 마세요.

아래 JSON으로만 응답:
{"roles": ["..."], "keywords": ["..."]}`;
}

export async function parseQuery(text: string, chat: ChatFn = defaultChat): Promise<SearchSpec> {
  try {
    const res = await chat({
      model: MODEL,
      messages: [{ role: 'user', content: buildPrompt(text) }],
      format: SCHEMA as any,
      think: false,
      options: { temperature: 0 },
    });
    const parsed = parseJsonLenient(res.message.content ?? '');
    const roles: unknown[] = Array.isArray(parsed.roles) ? parsed.roles : [];
    const tagIds = [
      ...new Set(
        roles
          .filter((r): r is string => typeof r === 'string')
          .map(nameToTagId)
          .filter((id): id is number => typeof id === 'number'),
      ),
    ];
    const keywords = Array.isArray(parsed.keywords)
      ? parsed.keywords.filter((k: unknown): k is string => typeof k === 'string' && k.trim().length > 0)
      : [];
    return { tagIds, keywords };
  } catch {
    return { tagIds: [], keywords: [text.trim()] };
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx tsx --test src/queryParser.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/queryParser.ts src/queryParser.test.ts
git commit -m "feat: queryParser로 자유텍스트→SearchSpec 추출(gemma+폴백)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: wanted.ts — SearchSpec 기반 수집

**Files:**
- Modify: `src/wanted.ts`
- Test: `src/wanted.test.ts` (신규 — 순수 함수 대상)

**Interfaces:**
- Consumes: `SearchSpec`(types), `DEFAULT_SEARCH`(jobTags).
- Produces:
  - `buildJobListUrl(limit: number, search: SearchSpec): string` (export, 순수)
  - `fetchJobList(limit?: number, search?: SearchSpec): Promise<WantedJob[]>`
  - `fetchJobsWithDetails(limit?: number, excludeIds?: Set<number>, search?: SearchSpec): Promise<WantedJob[]>`
- 참고(Task 1): `query` 파라미터가 서버사이드 narrowing을 하므로 키워드는 `query`로만 위임. 수집 후 클라이언트 키워드 필터는 두지 않는다.

- [ ] **Step 1: 실패 테스트 작성** — `src/wanted.test.ts`

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildJobListUrl } from './wanted';

test('buildJobListUrl: tagIds를 대괄호 없는 tag_type_ids로 반복 전달', () => {
  const url = buildJobListUrl(40, { tagIds: [872, 655], keywords: [] });
  const qs = new URL(url).searchParams.getAll('tag_type_ids');
  assert.deepEqual(qs, ['872', '655']);
  assert.equal(new URL(url).searchParams.has('tag_type_ids[]'), false);
});

test('buildJobListUrl: keywords 있으면 query 파라미터로 위임(서버사이드 narrowing)', () => {
  const url = buildJobListUrl(40, { tagIds: [872], keywords: ['자바', 'spring'] });
  assert.equal(new URL(url).searchParams.get('query'), '자바 spring');
});

test('buildJobListUrl: keywords 없으면 query 없음', () => {
  const url = buildJobListUrl(40, { tagIds: [872], keywords: [] });
  assert.equal(new URL(url).searchParams.has('query'), false);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/wanted.test.ts`
Expected: FAIL (`buildJobListUrl is not a function`)

- [ ] **Step 3: wanted.ts 수정 — import·상수**

`src/wanted.ts:1`을 아래로 교체:
```ts
import { WantedJob, SearchSpec } from './types';
import { DEFAULT_SEARCH } from './jobTags';
```
`src/wanted.ts:15-17`의 주석+`TAG_IDS` 상수를 **삭제**(직군은 이제 SearchSpec에서 옴).

- [ ] **Step 4: buildJobListUrl 추출 + fetchJobList 시그니처 변경**

`src/wanted.ts`의 `fetchJobList`(51-89행)를 아래로 교체:
```ts
export function buildJobListUrl(limit: number, search: SearchSpec): string {
  const params = new URLSearchParams({
    job_sort: 'job.latest_order',
    years: EXPERIENCE_YEARS,
    limit: String(limit),
    country: 'kr',
  });
  // ⚠️ 원티드 API는 'tag_type_ids[]'(대괄호)를 무시한다. 대괄호 없이 반복 전달해야 필터 적용.
  // 호출자(서버/CLI)가 tagIds 최소 1개를 보장한다(빈 태그 = 전체 수집 경로는 없음).
  search.tagIds.forEach((id) => params.append('tag_type_ids', String(id)));
  if (search.keywords.length) params.append('query', search.keywords.join(' '));
  return `${BASE_URL}/jobs?${params.toString()}`;
}

export async function fetchJobList(
  limit: number = 40,
  search: SearchSpec = DEFAULT_SEARCH,
): Promise<WantedJob[]> {
  const url = buildJobListUrl(limit, search);
  console.log(`📡 GET ${url}\n`);

  const res = await fetch(url, { headers: HEADERS });
  if (!res.ok) {
    throw new Error(`Wanted API error: ${res.status} ${res.statusText}`);
  }
  const body = (await res.json()) as { data?: any[] };
  const list = Array.isArray(body.data) ? body.data : [];
  if (list.length === 0) {
    console.warn('⚠️  공고가 0건입니다. API 응답 구조나 파라미터를 확인하세요.');
  }
  return list.map((job: any) => ({
    id: job.id,
    position: job.position ?? '',
    companyName: job.company?.name ?? '',
    location: job.address?.location ?? '',
    mainTasks: '',
    requirements: '',
    preferredPoints: '',
  }));
}
```

- [ ] **Step 5: fetchJobsWithDetails 시그니처 변경(searchSpec 전달)**

`src/wanted.ts`의 `fetchJobsWithDetails`(115-172행)를 아래로 교체:
(키워드는 `fetchJobList`의 `query`로 서버에서 이미 좁혀지므로 수집 후 키워드 필터는 두지 않는다.)
```ts
export async function fetchJobsWithDetails(
  limit: number = 40,
  excludeIds: Set<number> = new Set(),
  search: SearchSpec = DEFAULT_SEARCH,
): Promise<WantedJob[]> {
  console.log('📡 Fetching job list from Wanted...');
  // 병역특례 필터 후에도 limit개 확보하도록 여유분 요청
  const fetchLimit = Math.ceil(limit * 1.3);
  const jobs = await fetchJobList(fetchLimit, search);

  const titleFiltered = jobs
    .filter((j) => !EXCLUDE_KEYWORDS.some((kw) => j.position.includes(kw)))
    .filter((j) => !excludeIds.has(j.id));
  console.log(`✅ Found ${jobs.length} jobs (제목·확인함 필터 후 ${titleFiltered.length}개)\n`);

  const cache = loadCache();
  const results: WantedJob[] = [];
  let cacheHits = 0;
  let excluded = 0;

  for (let i = 0; i < titleFiltered.length; i++) {
    if (results.length >= limit) break;
    const job = titleFiltered[i];
    process.stdout.write(
      `  Fetching detail [${i + 1}/${titleFiltered.length}]: ${job.position} @ ${job.companyName}...         \r`,
    );
    const cached = cache[String(job.id)];
    const merged = cached ? { ...job, ...cached } : job;
    if (!cached) {
      try {
        const detail = await fetchJobDetail(job.id);
        cache[String(job.id)] = detail;
        Object.assign(merged, detail);
      } catch {
        /* 실패해도 기본 정보로 진행 */
      }
      await sleep(250);
    } else {
      cacheHits++;
    }
    if (isMilitaryAlternative(merged as WantedJob)) {
      excluded++;
      continue;
    }
    results.push(merged as WantedJob);
  }

  saveCache(cache);
  console.log(`\n✅ Detail fetching complete (cache hits: ${cacheHits}, 병역특례 제외: ${excluded}, 최종: ${results.length}개)\n`);
  return results;
}
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `npx tsx --test src/wanted.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 7: 커밋**

```bash
git add src/wanted.ts src/wanted.test.ts
git commit -m "feat: wanted 수집을 SearchSpec(태그+키워드) 기반으로 전환

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: pipeline.ts — searchSpec 스레딩

**Files:**
- Modify: `src/pipeline.ts`
- Test: `src/pipeline.test.ts` (케이스 추가)

**Interfaces:**
- Consumes: `SearchSpec`/`DEFAULT_SEARCH`.
- Produces:
  - `PipelineDeps.fetchJobs: (limit, excludeIds, search) => Promise<WantedJob[]>`
  - `runPipeline(resume, limit, onProgress, deps?, excludeIds?, search?)` — `search` 기본 `DEFAULT_SEARCH`

- [ ] **Step 1: 실패 테스트 추가** — `src/pipeline.test.ts` 끝에

```ts
import { DEFAULT_SEARCH } from './jobTags';
import { SearchSpec } from './types';

test('runPipeline: searchSpec을 fetchJobs로 전달', async () => {
  let received: SearchSpec | undefined;
  const fakeDeps: PipelineDeps = {
    fetchJobs: async (_l, _e, search) => { received = search; return []; },
    scoreJobs: async () => [],
  };
  const spec: SearchSpec = { tagIds: [872], keywords: ['자바'] };
  await runPipeline('이력서', 10, () => {}, fakeDeps, new Set(), spec);
  assert.deepEqual(received, spec);
});

test('runPipeline: searchSpec 미전달 시 DEFAULT_SEARCH', async () => {
  let received: SearchSpec | undefined;
  const fakeDeps: PipelineDeps = {
    fetchJobs: async (_l, _e, search) => { received = search; return []; },
    scoreJobs: async () => [],
  };
  await runPipeline('이력서', 10, () => {}, fakeDeps);
  assert.deepEqual(received, DEFAULT_SEARCH);
});
```

기존 `fetchJobs` 시그니처 사용 테스트(48-72행)는 3번째 인자를 무시하므로 그대로 통과한다.

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/pipeline.test.ts`
Expected: FAIL (새 두 테스트 — `received`가 undefined / 시그니처 불일치)

- [ ] **Step 3: pipeline.ts 수정**

`src/pipeline.ts:5-6`의 `PipelineDeps.fetchJobs` 타입:
```ts
  fetchJobs: (limit: number, excludeIds: Set<number>, search: SearchSpec) => Promise<WantedJob[]>;
```
`src/pipeline.ts:3` import에 `SearchSpec` 추가, `DEFAULT_SEARCH` import 추가:
```ts
import { WantedJob, ScoredJob, ProgressEvent, SearchSpec } from './types';
import { DEFAULT_SEARCH } from './jobTags';
```
`runPipeline` 시그니처(20-26행)에 `search` 추가 + 호출부(29행) 전달:
```ts
export async function runPipeline(
  resume: string,
  limit: number,
  onProgress: (e: ProgressEvent) => void,
  deps: PipelineDeps = defaultDeps,
  excludeIds: Set<number> = new Set(),
  search: SearchSpec = DEFAULT_SEARCH,
): Promise<ScoredJob[]> {
  try {
    onProgress({ type: 'status', message: '공고 수집 중...' });
    const jobs = await deps.fetchJobs(limit, excludeIds, search);
```

- [ ] **Step 4: 테스트 통과 확인(회귀 포함)**

Run: `npx tsx --test src/pipeline.test.ts`
Expected: PASS (기존 4 + 신규 2)

- [ ] **Step 5: 커밋**

```bash
git add src/pipeline.ts src/pipeline.test.ts
git commit -m "feat: runPipeline에 searchSpec 인자 스레딩

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: validation.ts — 검색 입력 검증

**Files:**
- Modify: `src/validation.ts`
- Test: `src/validation.test.ts` (케이스 추가)

**Interfaces:**
- Consumes: `isKnownTagId`(jobTags).
- Produces:
  - `sanitizeTagIds(raw: unknown): number[]` — 배열이 아니거나 미지의 ID는 버리고, 알려진 정수 ID만 고유하게 반환
  - `isValidSearch(queryText: unknown, tagIds: number[]): boolean` — 비어있지 않은 문자열 queryText 또는 1개 이상 tagIds면 true

- [ ] **Step 1: 실패 테스트 추가** — `src/validation.test.ts` 끝에

```ts
import { sanitizeTagIds, isValidSearch } from './validation';

test('sanitizeTagIds: 알려진 정수 ID만, 고유하게', () => {
  assert.deepEqual(sanitizeTagIds([872, 872, 655]), [872, 655]);
});

test('sanitizeTagIds: 미지의 ID·비정수·비배열 제거', () => {
  assert.deepEqual(sanitizeTagIds([99999, '872', 1.5]), []);
  assert.deepEqual(sanitizeTagIds('nope'), []);
  assert.deepEqual(sanitizeTagIds(undefined), []);
});

test('isValidSearch: queryText 있으면 true', () => {
  assert.equal(isValidSearch('백엔드', []), true);
});

test('isValidSearch: tagIds 있으면 true', () => {
  assert.equal(isValidSearch('', [872]), true);
  assert.equal(isValidSearch(undefined, [872]), true);
});

test('isValidSearch: 둘 다 비면 false', () => {
  assert.equal(isValidSearch('   ', []), false);
  assert.equal(isValidSearch(undefined, []), false);
});
```

(파일 상단에 `sanitizeTagIds, isValidSearch`가 기존 import 줄에 합쳐지도록 추가. 기존 import가 없으면 새 줄로.)

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/validation.test.ts`
Expected: FAIL (`sanitizeTagIds is not a function`)

- [ ] **Step 3: validation.ts 구현 — 끝에 추가**

```ts
import { isKnownTagId } from './jobTags';

// 클라이언트가 보낸 칩 tagIds를 신뢰하지 않고 정제: 알려진 정수 ID만 고유하게.
export function sanitizeTagIds(raw: unknown): number[] {
  if (!Array.isArray(raw)) return [];
  const out: number[] = [];
  for (const v of raw) {
    if (typeof v === 'number' && Number.isInteger(v) && isKnownTagId(v) && !out.includes(v)) {
      out.push(v);
    }
  }
  return out;
}

// 검색은 비어있지 않은 자유텍스트 또는 1개 이상의 직군 칩이 있어야 유효.
export function isValidSearch(queryText: unknown, tagIds: number[]): boolean {
  const hasText = typeof queryText === 'string' && queryText.trim().length > 0;
  return hasText || tagIds.length > 0;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx tsx --test src/validation.test.ts`
Expected: PASS (기존 + 신규 5)

- [ ] **Step 5: 커밋**

```bash
git add src/validation.ts src/validation.test.ts
git commit -m "feat: 검색 입력 검증(sanitizeTagIds, isValidSearch) 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 8: server.ts — /run searchSpec 구성 + /tags

**Files:**
- Modify: `src/server.ts`
- Test: `src/server.test.ts` (케이스 추가/수정)

**Interfaces:**
- Consumes: `parseQuery`(queryParser), `sanitizeTagIds`/`isValidSearch`(validation), `tagOptions`(jobTags), `SearchSpec`(types).
- Produces:
  - `ServerDeps.parseQuery: typeof parseQuery`
  - `GET /tags` → `{ tags: { id, label }[] }`
  - `POST /run` body: `{ resume, limit, queryText?, tagIds? }` — searchSpec 구성 후 runPipeline 6번째 인자로 전달

- [ ] **Step 1: 실패 테스트 추가** — `src/server.test.ts`

```ts
test('GET /tags: 직군 옵션 반환', async () => {
  const { url, close } = await listen({ runPipeline: (async () => []) as any, parseQuery: (async () => ({ tagIds: [], keywords: [] })) as any });
  try {
    const res = await fetch(`${url}/tags`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body.tags) && body.tags.length > 0);
    assert.ok(body.tags.every((t: any) => typeof t.id === 'number' && typeof t.label === 'string'));
  } finally {
    await close();
  }
});

test('POST /run: 검색 입력(텍스트·칩) 둘 다 없으면 400', async () => {
  const { url, close } = await listen({ runPipeline: (async () => []) as any, parseQuery: (async () => ({ tagIds: [], keywords: [] })) as any });
  try {
    const res = await fetch(`${url}/run`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5 }),
    });
    assert.equal(res.status, 400);
  } finally {
    await close();
  }
});

test('POST /run: queryText는 parseQuery로, 칩 tagIds와 병합해 runPipeline에 전달', async () => {
  withTempStore();
  let received: any;
  const fakeRun = (async (_r: string, _l: number, onProgress: any, _d: any, _e: any, search: any) => {
    received = search; onProgress({ type: 'done', count: 0 }); return [];
  }) as any;
  const fakeParse = (async () => ({ tagIds: [839], keywords: ['자바'] })) as any;
  const { url, close } = await listen({ runPipeline: fakeRun, parseQuery: fakeParse });
  try {
    const res = await fetch(`${url}/run`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5, queryText: 'ai 자바', tagIds: [872] }),
    });
    await res.text();
    assert.deepEqual([...received.tagIds].sort((a: number, b: number) => a - b), [839, 872]);
    assert.deepEqual(received.keywords, ['자바']);
  } finally {
    await close();
  }
});

test('POST /run: 텍스트는 있으나 직군 인식 0개·칩 없음이면 400(상황 B)', async () => {
  withTempStore();
  const fakeRun = (async () => []) as any;
  const fakeParse = (async () => ({ tagIds: [], keywords: ['자바'] })) as any; // 직군 못 뽑음
  const { url, close } = await listen({ runPipeline: fakeRun, parseQuery: fakeParse });
  try {
    const res = await fetch(`${url}/run`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5, queryText: '자바 공고' }),
    });
    assert.equal(res.status, 400);
  } finally {
    await close();
  }
});

test('POST /run: 칩만 있으면 parseQuery 호출 없이 그 태그로', async () => {
  withTempStore();
  let parseCalled = false;
  let received: any;
  const fakeRun = (async (_r: string, _l: number, onProgress: any, _d: any, _e: any, search: any) => {
    received = search; onProgress({ type: 'done', count: 0 }); return [];
  }) as any;
  const fakeParse = (async () => { parseCalled = true; return { tagIds: [], keywords: [] }; }) as any;
  const { url, close } = await listen({ runPipeline: fakeRun, parseQuery: fakeParse });
  try {
    const res = await fetch(`${url}/run`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5, tagIds: [655] }),
    });
    await res.text();
    assert.equal(parseCalled, false);
    assert.deepEqual(received.tagIds, [655]);
  } finally {
    await close();
  }
});
```

기존 `listen()` 호출들(검색 없는 `/run` 테스트들)은 이제 400을 받게 되므로 **수정 필요**: 기존 `POST /run` 테스트의 body에 `tagIds: [872]`를 추가하고, `listen({...})`에 `parseQuery: (async () => ({ tagIds: [], keywords: [] })) as any`를 추가한다. (해당 테스트들: 35행 "실행 중 409", 86행 "resumeHash", 108행 "seen 주입".)

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/server.test.ts`
Expected: FAIL (신규 케이스 + ServerDeps에 parseQuery 없음/타입 에러)

- [ ] **Step 3: server.ts 수정 — import + ServerDeps**

`src/server.ts:3-7` 근처에 추가:
```ts
import { runPipeline } from './pipeline';
import { parseQuery } from './queryParser';
import { clampLimit, isValidResume, isValidSearch, sanitizeTagIds } from './validation';
import { tagOptions } from './jobTags';
import { SearchSpec } from './types';
```
`ServerDeps`(9-13행):
```ts
export interface ServerDeps {
  runPipeline: typeof runPipeline;
  parseQuery: typeof parseQuery;
}
const defaultDeps: ServerDeps = { runPipeline, parseQuery };
```

- [ ] **Step 4: GET /tags 추가**

`createApp` 안, `app.post('/run', ...)` 위에:
```ts
app.get('/tags', (_req, res) => {
  res.json({ tags: tagOptions() });
});
```

- [ ] **Step 5: /run 본문 — searchSpec 구성**

`src/server.ts:23-34`의 검증부를 교체:
```ts
app.post('/run', async (req, res) => {
  const body = req.body ?? {};
  if (!isValidResume(body.resume)) {
    res.status(400).json({ error: '이력서를 입력하세요.' });
    return;
  }
  const tagIds = sanitizeTagIds(body.tagIds);
  if (!isValidSearch(body.queryText, tagIds)) {
    res.status(400).json({ error: '검색어를 입력하거나 직군을 선택하세요.' });
    return;
  }
  if (running) {
    res.status(409).json({ error: '이미 실행 중입니다.' });
    return;
  }

  running = true;
  try {
    // 자유텍스트가 있으면 LLM 파싱, 칩 태그와 병합. 칩만 있으면 LLM 생략.
    let search: SearchSpec = { tagIds, keywords: [] };
    if (typeof body.queryText === 'string' && body.queryText.trim()) {
      const parsed = await deps.parseQuery(body.queryText);
      search = {
        tagIds: [...new Set([...tagIds, ...parsed.tagIds])],
        keywords: parsed.keywords,
      };
    }
    // 상황 B: 텍스트는 있었으나 직군을 못 뽑고 칩도 없어 태그 0개 → 차단·안내(헤더 전송 전).
    if (search.tagIds.length === 0) {
      res.status(400).json({ error: '직군을 인식하지 못했어요. 직군 칩을 선택하거나 직군명을 포함해 다시 입력하세요.' });
      return;
    }
    const resumeHash = hashResume(body.resume);
    const excludeIds = getSeen(resumeHash);
    res.writeHead(200, {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      Connection: 'keep-alive',
    });
    const send = (e: ProgressEvent) =>
      res.write(formatSSE(e.type === 'done' ? { ...e, resumeHash } : e));
    await deps.runPipeline(body.resume, clampLimit(body.limit), send, undefined, excludeIds, search);
  } catch {
    if (!res.headersSent) res.status(500).json({ error: '실행 준비 실패' });
  } finally {
    running = false;
    if (!res.writableEnded) res.end();
  }
});
```
(주의: `parseQuery`가 잠금 안에서 실행되며, 실패해도 자체 폴백으로 throw하지 않는다. 그래도 다른 예외 대비 try 안에 둔다.)

- [ ] **Step 6: 테스트 통과 확인(회귀 포함)**

Run: `npx tsx --test src/server.test.ts`
Expected: PASS (수정한 기존 + 신규 4)

- [ ] **Step 7: 커밋**

```bash
git add src/server.ts src/server.test.ts
git commit -m "feat: /run에 searchSpec 구성·/tags 엔드포인트 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 9: 프론트엔드 — 쿼리 입력 + 직군 칩

**Files:**
- Modify: `public/index.html`, `public/app.js`, `public/style.css`(칩 스타일)
- Test: 없음(브라우저 수동 검증 — Step 5)

**Interfaces:**
- Consumes: `GET /tags`, `POST /run`(body에 `queryText`, `tagIds` 추가).

- [ ] **Step 1: index.html — 쿼리 입력칸 + 칩 컨테이너 추가**

`public/index.html:22`(resume textarea) 다음, `<div class="run-row">`(24행) 앞에 추가:
```html
      <label class="field-label" for="queryText">어떤 일을 찾으세요?</label>
      <input id="queryText" class="query-input" type="text"
        placeholder="예: 자바 잘하는 신입 백엔드 / 데이터 엔지니어 원해" />
      <div id="tagChips" class="tag-chips"></div>
```

- [ ] **Step 2: style.css — 칩 스타일 추가(파일 끝)**

```css
.query-input {
  width: 100%; padding: 10px 12px; margin-bottom: 10px;
  border: 1px solid #d1d5db; border-radius: 8px; font-size: 14px;
}
.tag-chips { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 12px; }
.chip {
  padding: 6px 12px; border: 1px solid #d1d5db; border-radius: 999px;
  background: #fff; font-size: 13px; cursor: pointer; user-select: none;
}
.chip.selected { background: #2f6fed; border-color: #2f6fed; color: #fff; }
```

- [ ] **Step 3: app.js — 칩 로딩·토글·payload·가드**

`public/app.js:2`(`let resumeHash`) 아래에 추가:
```js
const selectedTags = new Set();

async function loadTags() {
  try {
    const res = await fetch('/tags');
    const { tags } = await res.json();
    const root = $('tagChips');
    for (const t of tags) {
      const el = document.createElement('button');
      el.type = 'button';
      el.className = 'chip';
      el.textContent = t.label;
      el.dataset.id = t.id;
      el.addEventListener('click', () => {
        if (selectedTags.has(t.id)) { selectedTags.delete(t.id); el.classList.remove('selected'); }
        else { selectedTags.add(t.id); el.classList.add('selected'); }
      });
      root.appendChild(el);
    }
  } catch (err) {
    showError('직군 목록 로딩 실패: ' + String(err?.message ?? err));
  }
}
loadTags();
```

`run()` 함수(102-104행) 시작부의 가드를 교체:
```js
async function run() {
  const resume = $('resume').value.trim();
  if (!resume) { showError('이력서를 입력하세요.'); return; }
  const queryText = $('queryText').value.trim();
  if (!queryText && selectedTags.size === 0) {
    showError('검색어를 입력하거나 직군을 하나 이상 선택하세요.');
    return;
  }
```

`/run` fetch body(117행)를 교체:
```js
      body: JSON.stringify({ resume, limit: Number($('limit').value), queryText, tagIds: [...selectedTags] }),
```

- [ ] **Step 4: 정적 자산 회귀 — 서버 기동 확인**

Run: `npm run web` (백그라운드) 후 `curl -s localhost:3000/tags`
Expected: `{"tags":[...]}` JSON. (포트는 `src/serve.ts` 확인 후 맞출 것)

- [ ] **Step 5: 브라우저 수동 검증(메인 세션, Ollama 켜고)**

`npm run web` → 브라우저 접속 →
1. 빈 검색어 + 칩 미선택으로 분석 시도 → "검색어를 입력하거나..." 에러.
2. [백엔드][데이터엔지니어] 칩 선택 → 분석 → 해당 직군 공고 수집·채점되는지.
3. 자유텍스트 "자바 신입 백엔드" → 분석 → 결과가 자바/백엔드 쪽으로 좁혀지는지.
4. 칩+텍스트 동시 → 병합 동작.
관찰 결과를 사용자에게 보고.

- [ ] **Step 6: 커밋**

```bash
git add public/index.html public/app.js public/style.css
git commit -m "feat: 웹 UI에 자연어 쿼리 입력·직군 칩(다중선택) 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 10: 전체 회귀 + CLI 불변 확인

**Files:** 없음(검증 전용)

- [ ] **Step 1: 전체 테스트**

Run: `npm test`
Expected: 기존 25 + 신규(jobTags 4, jsonParse 4, queryParser 5, wanted 3, pipeline 2, validation 5, server 5) 전부 PASS.

- [ ] **Step 2: CLI 불변 확인(Ollama 켜고, 소량)**

Run: `npx tsx src/index.ts 3`
Expected: 기존과 동일하게 4직군 신입 공고 수집→채점→report.html 생성. 에러 없음.

- [ ] **Step 3: 이상 없으면 학습노트 작성(선택)**

`docs/learnings/2026-06-23-nl-query.md`에 3줄(놀란 점/다음엔 다르게/재사용 규칙).

- [ ] **Step 4: 최종 커밋(학습노트 작성 시)**

```bash
git add docs/learnings/2026-06-23-nl-query.md
git commit -m "docs: 조각 D 학습노트

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage:**
- 직군+키워드 쿼리 → Task 2(사전)·4(파싱)·5(수집) ✅
- LLM 추출+코드 매핑 → Task 4(parseQuery)+Task 2(nameToTagId) ✅
- 쿼리 필수 → Task 7(isValidSearch)·8(/run 400 상황 A·B)·9(프론트 가드) ✅
- 자유입력+다중선택 칩 → Task 8(병합)·9(칩 UI/토글) ✅
- 칩 다중선택 = 다직군 동시 → Task 5(tagIds 배열)·9(Set) ✅
- /tags로 칩 동기화 → Task 8 ✅
- CLI 불변 → Task 5/6 기본 인자(DEFAULT_SEARCH) + Task 10 Step 2 ✅
- 에러처리(상황 A 빈쿼리/상황 B 직군인식실패/파싱실패폴백) → Task 7·8(400×2)·4(폴백) ✅
- RAID(API query+tag 동시) → Task 1 프로브 + Task 5 수집 후 필터 보장 ✅
- 테스트 전략(jobTags/queryParser/server/wanted/pipeline) → 각 태스크 테스트 ✅

**빈 태그 정책:** 상황 A·B 모두 400 차단. `wanted.ts`에 도달하는 태그는 항상 ≥1(서버 보장). 무제한 수집 경로 없음. 설계 메모 + Task 8 Step 5에 명시.

**Placeholder scan:** Task 2의 프론트엔드 `id:669`는 Task 1 확정값으로 교체하라고 명시(인터태스크 의존). 그 외 플레이스홀더 없음.

**Type consistency:** `SearchSpec{tagIds,keywords}` 전 태스크 일관. `parseQuery` 반환·`fetchJobs(limit,excludeIds,search)`·`runPipeline(...,search)`·`ServerDeps.parseQuery` 시그니처 일치 확인.
