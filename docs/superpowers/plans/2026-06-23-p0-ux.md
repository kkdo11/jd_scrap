# P0 UX 개선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 웹 UI의 핵심 마찰 제거 — 이력서/검색조건 저장·복원, 서버측 진짜 중단, 빈/0건·에러 안내, 기본 공고수 15+예상시간.

**Architecture:** 중단은 `AbortSignal`을 server→pipeline→(wanted 상세fetch + scorer 채점)로 스레딩해 GPU 작업을 실제로 멈춘다. 나머지(저장/복원·빈상태·에러문구·ETA·토큰정리)는 프론트(`public/*`) 전용. 비주얼은 현재 프레임 유지.

**Tech Stack:** TypeScript, tsx, node:test, Express, 바닐라 JS/HTML/CSS, localStorage, AbortController.

## Global Constraints

- 커밋 prefix 영어 conventional(`feat:`/`fix:`/`refactor:`/`test:`/`docs:`), 본문 한국어 허용. 커밋 끝에 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. **푸시 금지**(명시 요청 시만).
- 테스트: `npm test`(= `tsx --test "src/**/*.test.ts"`). 단일: `npx tsx --test src/X.test.ts`. 백엔드 테스트는 네트워크/Ollama 비의존(주입·mock).
- **CLI(`src/index.ts`) 불변**: `signal` 미전달(기본 undefined)로 기존 동작 유지. 기존 55개 테스트 회귀 금지.
- 비주얼: **현재 프레임 유지**(라이트, `--primary:#3366ff`, Pretendard, 둥근 카드). 새 요소는 기존 CSS 변수/톤 사용. 레이아웃 개편 금지.
- `AbortController`/`AbortSignal`은 Node 18+ 기본 제공(현 런타임 OK).

## File Structure

| 파일 | 책임 | 항목 |
|---|---|---|
| `src/scorer.ts` | `scoreAllJobs`에 `signal` — 배치 루프 중단 | ② |
| `src/wanted.ts` | `fetchJobsWithDetails`에 `signal` — 상세 루프 중단 | ② |
| `src/pipeline.ts` | `runPipeline`/`PipelineDeps`에 `signal` 스레딩, scoreJobs 어댑터 | ② |
| `src/server.ts` | `/run`에 `AbortController` + `req.on('close')` → abort, signal 전달 | ② |
| `public/app.js` | persistence·중단 wiring·빈/0건·friendlyError·/tags 재시도·ETA | ①②③④⑤ |
| `public/index.html` | 중단 버튼·limit 기본15+힌트·`#emptyState` | ②③⑤ |
| `public/style.css` | 새 요소 스타일 + 토큰 일관화 | ⑥ |

## 시그니처 계약 (백엔드 ②)

```ts
// scorer.ts
scoreAllJobs(jobs, resume, onScored?, scoreOne? = scoreJob, signal?: AbortSignal): Promise<ScoredJob[]>
// wanted.ts
fetchJobsWithDetails(limit?, excludeIds?, search?, signal?: AbortSignal): Promise<WantedJob[]>
// pipeline.ts
PipelineDeps.fetchJobs: (limit, excludeIds, search, signal?: AbortSignal) => Promise<WantedJob[]>
PipelineDeps.scoreJobs: (jobs, resume, onScored?, signal?: AbortSignal) => Promise<ScoredJob[]>
runPipeline(resume, limit, onProgress, deps?, excludeIds?, search?, signal?: AbortSignal): Promise<ScoredJob[]>
```
- `scoreAllJobs`의 `signal`은 **scoreOne 뒤 5번째**(기존 호출 깨지 않음). `PipelineDeps.scoreJobs`는 `signal`을 4번째로 노출하므로 `defaultDeps.scoreJobs`를 어댑터로 감싼다(아래 Task 3).

## 병렬 메모(컨트롤러용)
- Wave1: Task 1(scorer) ∥ Task 2(wanted) — 파일 독립.
- Task 3(pipeline)은 1·2 머지 후. Task 4(server)는 3 후.
- 프론트 Task 5~8은 `public/*` 공유라 **순차**(병렬 금지). 백엔드 레인과는 파일이 겹치지 않아(src vs public) 교차 병렬 가능.

---

### Task 1: scorer — scoreAllJobs 중단 신호

**Files:**
- Modify: `src/scorer.ts` (`scoreAllJobs` 시그니처 + 배치 루프)
- Test: `src/scorer.test.ts` (케이스 추가)

**Interfaces:**
- Produces: `scoreAllJobs(jobs, resume, onScored?, scoreOne? = scoreJob, signal?: AbortSignal)` — `signal.aborted`면 남은 배치 중단, 이미 채점된 결과만(정렬해) 반환.

- [ ] **Step 1: 실패 테스트 추가** — `src/scorer.test.ts` 끝에

```ts
import { scoreAllJobs } from './scorer';
import { WantedJob, ScoredJob } from './types';

function wjob(id: number): WantedJob {
  return { id, position: `p${id}`, companyName: `c${id}`, location: '', mainTasks: '', requirements: '', preferredPoints: '' };
}
const fakeScore = async (job: WantedJob): Promise<ScoredJob> => ({ ...job, score: 50, matchPoints: [], gaps: [], summary: '' });

test('scoreAllJobs: 이미 abort된 signal이면 채점 0회', async () => {
  const ac = new AbortController();
  ac.abort();
  let calls = 0;
  const counting = async (j: WantedJob) => { calls++; return fakeScore(j); };
  const out = await scoreAllJobs([wjob(1), wjob(2)], '이력서', undefined, counting, ac.signal);
  assert.equal(calls, 0);
  assert.equal(out.length, 0);
});

test('scoreAllJobs: 중간 abort 시 처리된 것만 반환', async () => {
  const ac = new AbortController();
  let calls = 0;
  const abortingAfterFirst = async (j: WantedJob): Promise<ScoredJob> => {
    calls++;
    if (calls === 1) ac.abort(); // 첫 공고 처리 중 중단 신호
    return fakeScore(j);
  };
  const out = await scoreAllJobs([wjob(1), wjob(2), wjob(3)], '이력서', undefined, abortingAfterFirst, ac.signal);
  assert.equal(calls, 1);       // 2번째 배치 진입 전 break
  assert.equal(out.length, 1);
});
```
(상단에 `import { test }`/`assert`가 이미 있으면 재선언 말 것 — 기존 import 재사용. `WantedJob`/`ScoredJob`/`scoreAllJobs` import만 없으면 추가.)

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/scorer.test.ts`
Expected: FAIL (현재는 abort 무시하고 전부 채점 → calls=2/3, out 길이 불일치)

- [ ] **Step 3: scoreAllJobs 수정** — 시그니처에 `signal` 추가 + 루프 상단 가드

`src/scorer.ts`의 `scoreAllJobs` 시그니처를 교체:
```ts
export async function scoreAllJobs(
  jobs: WantedJob[],
  resume: string,
  onScored?: (e: { index: number; total: number; job: ScoredJob }) => void,
  scoreOne: (job: WantedJob, resume: string) => Promise<ScoredJob> = scoreJob,
  signal?: AbortSignal,
): Promise<ScoredJob[]> {
```
배치 `for` 루프(`for (let i = 0; i < jobs.length; i += CONCURRENCY) {`) 바로 다음 줄에 추가:
```ts
    if (signal?.aborted) break;
```

- [ ] **Step 4: 테스트 통과 확인(회귀 포함)**

Run: `npx tsx --test src/scorer.test.ts`
Expected: PASS (기존 + 신규 2)

- [ ] **Step 5: 커밋**

```bash
git add src/scorer.ts src/scorer.test.ts
git commit -m "feat: scoreAllJobs에 AbortSignal 중단 지원

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: wanted — fetchJobsWithDetails 중단 신호

**Files:**
- Modify: `src/wanted.ts` (`fetchJobsWithDetails` 시그니처 + 상단/루프 가드)
- Test: `src/wanted.test.ts` (케이스 추가)

**Interfaces:**
- Produces: `fetchJobsWithDetails(limit?, excludeIds?, search?, signal?: AbortSignal)` — 시작 시 abort면 즉시 `[]`(fetch 0회), 상세 루프 중 abort면 조기 종료.

- [ ] **Step 1: 실패 테스트 추가** — `src/wanted.test.ts` 끝에

```ts
import { fetchJobsWithDetails } from './wanted';
import { DEFAULT_SEARCH } from './jobTags';

test('fetchJobsWithDetails: 이미 abort면 fetch 없이 빈 배열', async () => {
  const orig = globalThis.fetch;
  let fetched = 0;
  globalThis.fetch = (async () => { fetched++; throw new Error('should not fetch'); }) as any;
  try {
    const ac = new AbortController();
    ac.abort();
    const out = await fetchJobsWithDetails(5, new Set(), DEFAULT_SEARCH, ac.signal);
    assert.deepEqual(out, []);
    assert.equal(fetched, 0);
  } finally {
    globalThis.fetch = orig;
  }
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/wanted.test.ts`
Expected: FAIL (현재는 signal 무시 → fetchJobList 호출로 `fetched`≥1 또는 throw)

- [ ] **Step 3: fetchJobsWithDetails 수정** — 시그니처 + 가드 2곳

`src/wanted.ts`의 `fetchJobsWithDetails` 시그니처를 교체:
```ts
export async function fetchJobsWithDetails(
  limit: number = 40,
  excludeIds: Set<number> = new Set(),
  search: SearchSpec = DEFAULT_SEARCH,
  signal?: AbortSignal,
): Promise<WantedJob[]> {
  if (signal?.aborted) return [];
  console.log('📡 Fetching job list from Wanted...');
```
상세 루프 `for (let i = 0; i < titleFiltered.length; i++) {` 바로 다음(기존 `if (results.length >= limit) break;` 위 또는 아래)에 추가:
```ts
    if (signal?.aborted) break;
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx tsx --test src/wanted.test.ts`
Expected: PASS (기존 5 + 신규 1)

- [ ] **Step 5: 커밋**

```bash
git add src/wanted.ts src/wanted.test.ts
git commit -m "feat: fetchJobsWithDetails에 AbortSignal 중단 지원

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: pipeline — signal 스레딩 + scoreJobs 어댑터

> ⚠️ Task 1·2 머지 후 진행(scorer/wanted 시그니처에 의존).

**Files:**
- Modify: `src/pipeline.ts`
- Test: `src/pipeline.test.ts` (케이스 추가)

**Interfaces:**
- Consumes: `scoreAllJobs(…, signal?)`(Task 1), `fetchJobsWithDetails(…, signal?)`(Task 2).
- Produces:
  - `PipelineDeps.fetchJobs: (limit, excludeIds, search, signal?: AbortSignal) => Promise<WantedJob[]>`
  - `PipelineDeps.scoreJobs: (jobs, resume, onScored?, signal?: AbortSignal) => Promise<ScoredJob[]>`
  - `runPipeline(resume, limit, onProgress, deps?, excludeIds?, search?, signal?: AbortSignal)`

- [ ] **Step 1: 실패 테스트 추가** — `src/pipeline.test.ts` 끝에

```ts
test('runPipeline: signal을 fetchJobs와 scoreJobs로 전달', async () => {
  const ac = new AbortController();
  let fSignal: AbortSignal | undefined;
  let sSignal: AbortSignal | undefined;
  const fakeDeps: PipelineDeps = {
    fetchJobs: async (_l, _e, _s, signal) => { fSignal = signal; return [{ id: 1 } as WantedJob]; },
    scoreJobs: async (_j, _r, _o, signal) => { sSignal = signal; return []; },
  };
  await runPipeline('이력서', 5, () => {}, fakeDeps, new Set(), DEFAULT_SEARCH, ac.signal);
  assert.equal(fSignal, ac.signal);
  assert.equal(sSignal, ac.signal);
});
```
(`DEFAULT_SEARCH` import가 없으면 추가: `import { DEFAULT_SEARCH } from './jobTags';`)

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/pipeline.test.ts`
Expected: FAIL (현재 fetchJobs/scoreJobs는 signal 인자 없음 → undefined)

- [ ] **Step 3: pipeline.ts 수정**

`PipelineDeps` 교체:
```ts
export interface PipelineDeps {
  fetchJobs: (limit: number, excludeIds: Set<number>, search: SearchSpec, signal?: AbortSignal) => Promise<WantedJob[]>;
  scoreJobs: (
    jobs: WantedJob[],
    resume: string,
    onScored?: (e: { index: number; total: number; job: ScoredJob }) => void,
    signal?: AbortSignal,
  ) => Promise<ScoredJob[]>;
}
```
`defaultDeps` 교체(scoreJobs 어댑터로 scoreOne 자리 비우고 signal 전달):
```ts
const defaultDeps: PipelineDeps = {
  fetchJobs: fetchJobsWithDetails,
  scoreJobs: (jobs, resume, onScored, signal) => scoreAllJobs(jobs, resume, onScored, undefined, signal),
};
```
`runPipeline` 시그니처에 `signal` 추가 + 호출부 전달:
```ts
export async function runPipeline(
  resume: string,
  limit: number,
  onProgress: (e: ProgressEvent) => void,
  deps: PipelineDeps = defaultDeps,
  excludeIds: Set<number> = new Set(),
  search: SearchSpec = DEFAULT_SEARCH,
  signal?: AbortSignal,
): Promise<ScoredJob[]> {
  try {
    onProgress({ type: 'status', message: '공고 수집 중...' });
    const jobs = await deps.fetchJobs(limit, excludeIds, search, signal);

    onProgress({ type: 'status', message: `${jobs.length}개 공고 채점 시작` });
    const result = await deps.scoreJobs(jobs, resume, (e) =>
      onProgress({ type: 'scored', index: e.index, total: e.total, job: e.job }),
      signal,
    );

    onProgress({ type: 'done', count: result.length });
    return result;
  } catch (err: any) {
    onProgress({ type: 'error', message: String(err?.message ?? err) });
    throw err;
  }
}
```

- [ ] **Step 4: 테스트 통과 확인(회귀 포함)**

Run: `npx tsx --test src/pipeline.test.ts`
Expected: PASS (기존 + 신규 1). 기존 fakeDeps(인자 적게 받음)도 추가 인자 무시로 통과.

- [ ] **Step 5: 커밋**

```bash
git add src/pipeline.ts src/pipeline.test.ts
git commit -m "feat: runPipeline에 AbortSignal 스레딩(scoreJobs 어댑터)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: server — /run AbortController + 연결 종료 시 중단

> ⚠️ Task 3 머지 후.

**Files:**
- Modify: `src/server.ts` (`/run` 핸들러)
- Test: `src/server.test.ts` (케이스 추가)

**Interfaces:**
- Consumes: `runPipeline(…, signal?)`(Task 3).
- Produces: `/run` 동작 — 클라이언트 연결 종료(정상 완료 아님) 시 `runPipeline`에 전달한 signal을 abort → 잠금 해제.

- [ ] **Step 1: 실패 테스트 추가** — `src/server.test.ts` 끝에

```ts
test('POST /run: 스트림 중 클라이언트 연결 종료 → 잠금 해제(다음 요청 200)', async () => {
  withTempStore();
  // signal abort 시 반환하는 fakeRun (실제 중단 동작 모사)
  const fakeRun = (async (_r: string, _l: number, onProgress: any, _d: any, _e: any, _s: any, signal: AbortSignal) => {
    onProgress({ type: 'status', message: 'start' });
    await new Promise<void>((resolve) => {
      if (signal?.aborted) return resolve();
      signal?.addEventListener('abort', () => resolve());
    });
    return [];
  }) as any;
  const fakeParse = (async () => ({ tagIds: [], keywords: [] })) as any;
  const { url, close } = await listen({ runPipeline: fakeRun, parseQuery: fakeParse });
  try {
    // 요청1: 스트림 시작 후 클라이언트가 중단
    const ac = new AbortController();
    const p1 = fetch(`${url}/run`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5, tagIds: [872] }),
      signal: ac.signal,
    });
    await new Promise((r) => setTimeout(r, 50)); // 핸들러가 잠금 잡을 시간
    ac.abort();
    await p1.catch(() => {}); // abort로 reject되는 것 무시

    // 요청2: 잠금이 풀렸으면 200
    await new Promise((r) => setTimeout(r, 50));
    const req2 = await fetch(`${url}/run`, {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume: '이력서', limit: 5, tagIds: [872] }),
    });
    assert.equal(req2.status, 200);
    await req2.text();
  } finally {
    await close();
  }
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx tsx --test src/server.test.ts`
Expected: FAIL — 현재는 연결 종료를 안 잡아 fakeRun이 영원히 대기 → 잠금 고착 → 요청2가 409(또는 타임아웃).

- [ ] **Step 3: server.ts /run 수정** — AbortController + req close

`running = true;` 직후, `try {` 안의 맨 앞에 AbortController 준비. `src/server.ts`의 `/run` 핸들러에서 `running = true;`부터 `await deps.runPipeline(...)`까지를 아래로 교체:
```ts
    running = true;
    const ac = new AbortController();
    let finished = false;
    // 클라이언트가 스트림을 끊으면(정상 완료 아님) 진행 중 작업을 중단해 잠금을 즉시 해제.
    req.on('close', () => { if (!finished) ac.abort(); });
    try {
      // 자유텍스트가 있으면 LLM 파싱, 칩 태그와 병합. 칩만 있으면 LLM 생략.
      let search: SearchSpec = { tagIds, keywords: [] };
      if (typeof body.queryText === 'string' && body.queryText.trim()) {
        const parsed = await deps.parseQuery(body.queryText);
        search = {
          tagIds: [...new Set([...tagIds, ...parsed.tagIds])],
          keywords: parsed.keywords,
        };
      }
      // 상황 B: 텍스트는 있었으나 직군을 못 뽑고 칩도 없어 태그 0개 → 차단·안내(헤더 전송 전).
      if (search.tagIds.length === 0) {
        res.status(400).json({ error: '직군을 인식하지 못했어요. 직군 칩을 선택하거나 직군명을 포함해 다시 입력하세요.' });
        return;
      }
      const resumeHash = hashResume(body.resume);
      const excludeIds = getSeen(resumeHash);
      res.writeHead(200, {
        'Content-Type': 'text/event-stream',
        'Cache-Control': 'no-cache',
        Connection: 'keep-alive',
      });
      const send = (e: ProgressEvent) =>
        res.write(formatSSE(e.type === 'done' ? { ...e, resumeHash } : e));
      await deps.runPipeline(body.resume, clampLimit(body.limit), send, undefined, excludeIds, search, ac.signal);
    } catch {
      if (!res.headersSent) res.status(500).json({ error: '실행 준비 실패' });
    } finally {
      finished = true;
      running = false;
      if (!res.writableEnded) res.end();
    }
```
(주의: 상황 B의 `return`은 `try` 안 → `finally`에서 `finished=true`·잠금 해제. 정상 완료도 `finished=true`라 이후 close 이벤트가 abort 안 함.)

- [ ] **Step 4: 테스트 통과 확인(회귀 포함)**

Run: `npx tsx --test src/server.test.ts`
Expected: PASS (기존 + 신규 1).

- [ ] **Step 5: 전체 회귀**

Run: `npm test`
Expected: 기존 55 + scorer 2 + wanted 1 + pipeline 1 + server 1 = 60 PASS.

- [ ] **Step 6: 커밋**

```bash
git add src/server.ts src/server.test.ts
git commit -m "feat: /run 연결 종료 시 AbortController로 작업 중단·잠금 해제

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 프론트 — 이력서·검색조건 저장/복원 + 기본15 + 예상시간/ETA

**Files:**
- Modify: `public/app.js`, `public/index.html`
- Test: 없음(브라우저 수동 — Task 9에서 일괄 검증)

**Interfaces:**
- Consumes: `/tags`(칩), `selectedTags` Set(기존), `loadTags()`(기존).
- Produces: 페이지 로드 시 이력서/쿼리/공고수/칩 복원. limit 기본 15 + "약 N분 예상" + 실행 중 ETA.

- [ ] **Step 1: index.html — limit 기본값 15 + 힌트 span**

`public/index.html`의 limit input(`value="50"`)을 `value="15"`로 바꾸고, 그 뒤(같은 `.run-row` 안, 버튼 앞)에 힌트 추가:
```html
        <input id="limit" class="limit-input" type="number" min="1" max="100" value="15" />
        <span id="limitHint" class="limit-hint"></span>
        <button id="runBtn" class="run-btn">분석 시작</button>
```

- [ ] **Step 2: app.js — localStorage 저장/복원 + 힌트/ETA 로직**

`public/app.js` 상단(`const selectedTags = new Set();` 다음)에 추가:
```js
const LS = { resume: 'wm.resume', query: 'wm.query', tags: 'wm.tagIds', limit: 'wm.limit' };

function savePrefs() {
  try {
    localStorage.setItem(LS.query, $('queryText').value);
    localStorage.setItem(LS.tags, JSON.stringify([...selectedTags]));
    localStorage.setItem(LS.limit, $('limit').value);
  } catch { /* localStorage 불가 환경 무시 */ }
}
function restorePrefs() {
  try {
    const r = localStorage.getItem(LS.resume); if (r) $('resume').value = r;
    const q = localStorage.getItem(LS.query); if (q) $('queryText').value = q;
    const l = localStorage.getItem(LS.limit); if (l) $('limit').value = l;
  } catch { /* 무시 */ }
}
function restoreSelectedTags() {
  // 칩 렌더 후 호출: 저장된 tagIds로 selectedTags·.selected 복원
  let saved = [];
  try { saved = JSON.parse(localStorage.getItem(LS.tags) || '[]'); } catch { saved = []; }
  if (!Array.isArray(saved)) return;
  for (const id of saved) {
    selectedTags.add(id);
    const chip = $('tagChips').querySelector(`.chip[data-id="${id}"]`);
    if (chip) chip.classList.add('selected');
  }
}

const EST_SEC_PER_JOB = 15; // 대략치(gemma 12b). ETA는 실행 중 경과시간으로 자동 보정.
function updateLimitHint() {
  const n = Number($('limit').value) || 0;
  const mins = Math.max(1, Math.ceil((n * EST_SEC_PER_JOB) / 60));
  $('limitHint').textContent = n > 0 ? `약 ${mins}분 예상` : '';
}
```
이력서 입력 디바운스 저장 + 힌트 초기화를 `loadTags();` 호출 줄 근처(파일 하단 이벤트 등록부 위)에 추가:
```js
let resumeSaveTimer;
$('resume').addEventListener('input', () => {
  clearTimeout(resumeSaveTimer);
  resumeSaveTimer = setTimeout(() => {
    try { localStorage.setItem(LS.resume, $('resume').value); } catch {}
  }, 400);
});
$('limit').addEventListener('input', updateLimitHint);
restorePrefs();
updateLimitHint();
```

- [ ] **Step 3: app.js — 칩 복원 호출 + run에서 savePrefs + ETA 표시**

`loadTags()`가 칩을 다 그린 뒤 복원하도록, `loadTags` 함수의 `for` 루프 종료 직후(`root.appendChild(el);` 루프 끝, `}` 다음)에 추가:
```js
    restoreSelectedTags();
```
`run()` 함수 안 가드 통과 직후(`setRunning(true);` 앞)에 추가:
```js
  savePrefs();
```
ETA: `run()` 시작부에 시작시각 기록, `handleChunk`의 `scored` 분기에서 ETA 계산. `run()`의 `setRunning(true);` 다음 줄에:
```js
  runStartedAt = Date.now();
```
상단 전역에 `let runStartedAt = 0;` 추가. `handleChunk`의 scored 분기를 교체:
```js
  else if (e.type === 'scored') {
    addCard(e.job);
    setProgress(e.index, e.total);
    const elapsed = (Date.now() - runStartedAt) / 1000;
    const remain = e.index > 0 ? Math.ceil((elapsed / e.index) * (e.total - e.index) / 60) : 0;
    setStatus(remain > 0 ? `채점 중 ${e.index}/${e.total} · 약 ${remain}분 남음` : `채점 중 ${e.index}/${e.total}`);
  }
```

- [ ] **Step 4: 구문 점검 + 커밋**

Run: `node --check public/app.js`
Expected: exit 0
```bash
git add public/app.js public/index.html
git commit -m "feat: 이력서·검색조건 localStorage 저장/복원, 기본 15+예상시간/ETA

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: 프론트 — 분석 중단 버튼(클라이언트 abort)

**Files:**
- Modify: `public/app.js`, `public/index.html`
- Test: 없음(수동 — Task 9)

**Interfaces:**
- Consumes: 서버 `/run` 연결 종료 시 중단(Task 4).
- Produces: 실행 중 "중단" 버튼 → 진행 중 fetch abort → SSE 종료 → 서버 잠금 해제. 받은 카드 유지.

- [ ] **Step 1: index.html — 중단 버튼 추가**

`runBtn` 다음에 추가(기본 숨김):
```html
        <button id="runBtn" class="run-btn">분석 시작</button>
        <button id="cancelBtn" class="cancel-btn" type="button" hidden>중단</button>
```

- [ ] **Step 2: app.js — AbortController 배선**

상단 전역에 `let runController = null;` 추가.
`setRunning(on)` 함수를 교체(중단 버튼 토글 포함):
```js
function setRunning(on) {
  $('runBtn').disabled = on;
  $('runBtn').textContent = on ? '분석 중...' : '분석 시작';
  $('cancelBtn').hidden = !on;
}
```
`run()`의 fetch를 controller로 묶기 — `setRunning(true);` 다음(또는 ETA 기록 근처)에:
```js
  runController = new AbortController();
```
fetch 호출에 `signal` 추가:
```js
    const res = await fetch('/run', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ resume, limit: Number($('limit').value), queryText, tagIds: [...selectedTags] }),
      signal: runController.signal,
    });
```
`run()`의 `catch (err)`를 중단 구분 처리로 교체:
```js
  } catch (err) {
    if (err?.name === 'AbortError') setStatus('중단됨');
    else showError(friendlyError(err));
  } finally {
    setRunning(false);
    runController = null;
  }
```
(`friendlyError`는 Task 7에서 추가. Task 6을 Task 7보다 먼저 머지하면 임시로 `String(err?.message ?? err)` 사용 후 Task 7에서 교체 — 컨트롤러는 7을 6 뒤에 배치.)
중단 버튼 핸들러를 파일 하단 이벤트 등록부에 추가:
```js
$('cancelBtn').addEventListener('click', () => { if (runController) runController.abort(); });
```

- [ ] **Step 3: 구문 점검 + 커밋**

Run: `node --check public/app.js`
Expected: exit 0
```bash
git add public/app.js public/index.html
git commit -m "feat: 분석 중단 버튼(클라이언트 abort→서버 중단)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: 프론트 — 빈/0건 상태 + friendlyError + /tags 인라인 재시도

> Task 6 뒤(같은 `catch`에서 `friendlyError` 사용).

**Files:**
- Modify: `public/app.js`, `public/index.html`
- Test: 없음(수동 — Task 9)

**Interfaces:**
- Produces: `friendlyError(raw)` 매퍼; 첫 진입/0건 안내(`#emptyState`); `/tags` 실패 시 칩 영역 인라인 메시지 + 재시도.

- [ ] **Step 1: index.html — emptyState 영역**

`<section id="results" class="results"></section>` 바로 위에 추가:
```html
    <div id="emptyState" class="empty-state">이력서를 넣고 직군·검색어를 고른 뒤 분석을 시작하세요.</div>
```

- [ ] **Step 2: app.js — friendlyError + emptyState 토글 + /tags 재시도**

상단(함수 영역)에 추가:
```js
function friendlyError(err) {
  const msg = String(err?.message ?? err);
  if (/Unexpected token|<!DOCTYPE|is not valid JSON/i.test(msg))
    return '서버 응답이 올바르지 않습니다. 서버를 재시작했는지 확인하세요(옛 서버가 떠 있을 수 있습니다).';
  if (/Failed to fetch|NetworkError|ECONNREFUSED/i.test(msg))
    return '서버에 연결할 수 없습니다. 서버가 실행 중인지 확인하세요.';
  return msg;
}
function showEmpty(html) {
  const el = $('emptyState');
  el.innerHTML = html;
  el.hidden = false;
}
function hideEmpty() { $('emptyState').hidden = true; }
```
`loadTags()`의 `catch`를 인라인 메시지+재시도로 교체:
```js
  } catch (err) {
    const root = $('tagChips');
    root.innerHTML = '<span class="chips-error">직군 목록을 불러오지 못했습니다. <button type="button" id="retryTags" class="link-btn">재시도</button></span>';
    root.querySelector('#retryTags').addEventListener('click', () => { root.innerHTML = ''; loadTags(); });
  }
```
`run()` 시작에서 결과 비울 때 emptyState도 숨김 — `$('results').innerHTML = '';` 다음에 `hideEmpty();` 추가.
`handleChunk`의 `done` 분기를 0건 안내 포함으로 교체:
```js
  else if (e.type === 'done') {
    resumeHash = e.resumeHash ?? null;
    sortCards();
    setProgress(1, 1);
    $('resultsBar').hidden = !resumeHash;
    if (e.count === 0) {
      setStatus('완료 — 0개');
      showEmpty('조건에 맞는 신입 공고를 찾지 못했어요.<br>· 키워드를 줄이거나 빼보세요<br>· 다른 직군 칩을 선택해보세요<br>· 공고 수를 늘려보세요');
    } else {
      setStatus(`완료 — ${e.count}개 공고`);
    }
  }
```
페이지 로드 시 첫 안내가 보이도록: `emptyState`는 기본 표시(첫 카드/실행 시 숨김). 이미 마크업에 텍스트가 있으니 별도 호출 불필요(첫 run에서 hideEmpty).
Task 6에서 임시로 둔 catch의 에러 표시를 `showError(friendlyError(err))`로 확정(이미 그렇게 작성했다면 변경 없음).

- [ ] **Step 3: 구문 점검 + 커밋**

Run: `node --check public/app.js`
Expected: exit 0
```bash
git add public/app.js public/index.html
git commit -m "feat: 빈/0건 안내·친화적 에러 메시지·/tags 인라인 재시도

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 8: 프론트 — 새 요소 스타일 + 토큰 일관화(현재 프레임 내)

**Files:**
- Modify: `public/style.css`
- Test: 없음(수동 — Task 9)

**Interfaces:**
- Produces: `.cancel-btn`/`.limit-hint`/`.empty-state`/`.chips-error`/`.link-btn` 스타일 + 기존 `.query-input`/`.chip`/`.reset-btn` 토큰 일관화.

- [ ] **Step 1: style.css — 새 요소 스타일 추가(파일 끝)**

```css
.cancel-btn {
  background: var(--surface); color: var(--muted);
  border: 1px solid var(--border); border-radius: 10px;
  padding: 11px 18px; font-size: 0.92rem; font-weight: 700; cursor: pointer;
}
.cancel-btn:hover { color: #c0392b; border-color: #f0c4c0; }
.limit-hint { font-size: 0.78rem; color: var(--muted); }
.empty-state {
  background: var(--surface); border: 1px dashed var(--border);
  border-radius: 14px; padding: 28px 20px; text-align: center;
  color: var(--muted); font-size: 0.9rem; line-height: 1.7;
}
.chips-error { font-size: 0.82rem; color: #c0392b; }
.link-btn {
  background: none; border: none; color: var(--primary);
  font: inherit; cursor: pointer; text-decoration: underline; padding: 0;
}
```

- [ ] **Step 2: style.css — 토큰 일관화(현재 프레임 유지, 색/단위만 통일)**

기존 규칙을 교체:
```css
.query-input {
  width: 100%; padding: 0.625rem 0.75rem; margin-bottom: 0.625rem;
  border: 1px solid var(--border); border-radius: 10px; font-size: 0.9rem; font-family: inherit;
}
.query-input:focus { outline: none; border-color: var(--primary); }
.tag-chips { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 0.75rem; }
.chip {
  padding: 0.375rem 0.75rem; border: 1px solid var(--border); border-radius: 999px;
  background: var(--surface); font-size: 0.8rem; color: var(--text);
  cursor: pointer; user-select: none; transition: background 0.15s, border-color 0.15s;
}
.chip:hover { border-color: var(--primary); }
.chip.selected { background: var(--primary); border-color: var(--primary); color: #fff; }
.reset-btn {
  padding: 0.375rem 0.75rem; border: 1px solid var(--border); border-radius: 8px;
  background: var(--surface); color: var(--text); font-size: 0.8rem; cursor: pointer;
}
.reset-btn:hover { background: var(--bg); }
```
(`.seen-toggle`의 `13px`/`#6b7280`도 `0.8rem`/`var(--muted)`로 통일.)

- [ ] **Step 3: 커밋**

```bash
git add public/style.css
git commit -m "refactor: 새 UX 요소 스타일 추가 + 칩/입력 토큰 일관화(프레임 유지)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 9: 전체 회귀 + 브라우저 수동 검증

**Files:** 없음(검증 전용)

- [ ] **Step 1: 전체 테스트**

Run: `npm test`
Expected: 60 PASS (55 기존 + 5 신규).

- [ ] **Step 2: 서버 기동 + 정적 점검**

Run: `PORT=3142 npm run web` (백그라운드) 후 `curl -s localhost:3142/tags`
Expected: `{"tags":[...]}`. `node --check public/app.js` exit 0.

- [ ] **Step 3: 브라우저 수동 검증(메인 세션, Ollama 켜고)**

체크리스트:
1. **저장/복원**: 이력서·쿼리·칩·공고수 입력 → 새로고침 → 모두 복원되는지.
2. **기본/힌트**: 첫 로드 시 공고수 15, "약 N분 예상" 표시, 값 변경 시 갱신.
3. **빈 상태**: 첫 진입 시 안내 문구 노출.
4. **중단**: 분석 시작 → 중단 클릭 → "중단됨", 즉시 재검색 가능(409 안 뜸).
5. **ETA**: 채점 중 "약 N분 남음" 표시.
6. **0건**: 매우 좁은 키워드로 실행 → 0건 안내 + 팁.
7. **에러**: (옛 서버 흉내 불필요) `/tags` 실패 시 인라인 재시도만 확인 가능하면 확인.
8. **비주얼**: 칩/버튼/카드가 기존 프레임과 일관(라이트·파랑·둥근).

관찰 결과를 컨트롤러/사용자에게 보고.

- [ ] **Step 4: 학습노트(선택)**

`docs/learnings/2026-06-23-p0-ux.md`에 3줄.

---

## Self-Review

**Spec coverage:**
- ① 저장/복원 → Task 5 ✅
- ② 서버측 중단 → Task 1(scorer)·2(wanted)·3(pipeline)·4(server)·6(프론트 abort) ✅
- ③ 빈/0건 → Task 7 ✅
- ④ 에러 친화화·/tags 재시도 → Task 7 ✅
- ⑤ 기본15·예상시간·ETA → Task 5 ✅
- ⑥ 비주얼 토큰 일관화 → Task 8 ✅
- 테스트 전략(scorer/wanted/pipeline/server abort) → Task 1·2·4 + pipeline 3 ✅

**Placeholder scan:** 없음. EST_SEC_PER_JOB=15는 대략치로 주석 명시(요구 아닌 상수). Task 6의 friendlyError 임시처리는 Task 7와의 순서로 해소(명시).

**Type consistency:** `signal?: AbortSignal` 전 태스크 일관. `scoreAllJobs(…, scoreOne?, signal?)`(5번째) vs `PipelineDeps.scoreJobs(…, onScored?, signal?)`(4번째)는 `defaultDeps.scoreJobs` 어댑터로 정합(Task 3). `runPipeline` 7번째 인자 `signal`, server가 7번째로 전달 — 일치.
